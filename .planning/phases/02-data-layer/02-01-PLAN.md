---
phase: 02-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/data/videos.json
  - src/lib/data/categories.ts
  - src/lib/data/schema.ts
  - src/lib/data/videos.ts
  - src/lib/data/index.ts
  - src/lib/data/categories.test.ts
  - src/lib/data/schema.test.ts
  - src/lib/data/videos.test.ts
  - src/lib/data/videos.json.test.ts
  - src/lib/data/.videos-source-sha
  - vite.config.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
gap_closure: false
must_haves:
  truths:
    - "src/lib/data/videos.json is byte-for-byte identical to ../michelle_ngo_four/src/lib/data/videos.json (sha256 match)"
    - "pnpm build succeeds with zero errors; the validateVideosPlugin runs at buildStart and parses videos.json against VideoArraySchema"
    - "pnpm test passes all four data-layer test files: categories.test.ts, schema.test.ts, videos.test.ts, videos.json.test.ts"
    - "Intentionally breaking a record (delete title, change source to 'tiktok', or duplicate (source,id)) fails pnpm build with a z.prettifyError message pointing at the offending row"
    - "import { videos, producerReelId, getById, getByCategory, getCategoriesInDisplayOrder, getCategoriesWithCounts, CATEGORIES, categoryToSlug, slugToCategory, type Video, type Category } from '$lib/data' compiles unchanged (11-name surface matches _four)"
    - "src/lib/data/.videos-source-sha exists with three lines: four_commit_sha=<40hex>, videos_json_sha256=<64hex>, synced_at=<ISO date>"
  artifacts:
    - path: "src/lib/data/videos.json"
      provides: "56-video catalog byte-identical to _four (DATA-01)"
      contains: '"source": "vimeo"'
    - path: "src/lib/data/categories.ts"
      provides: "8-category CATEGORIES const + categoryToSlug + slugToCategory (D-21 mirror)"
      exports: ["CATEGORIES", "categoryToSlug", "slugToCategory", "Category"]
    - path: "src/lib/data/schema.ts"
      provides: "VideoSchema + VideoArraySchema with z.strictObject + z.discriminatedUnion('source', ...) + z.iso.date() (DATA-02)"
      exports: ["VideoSchema", "VideoArraySchema", "CategorySchema", "Video"]
    - path: "src/lib/data/videos.ts"
      provides: "Typed loader with producerReelId='264677021' + getById + getByCategory + getCategoriesInDisplayOrder + getCategoriesWithCounts (DATA-03)"
      exports: ["videos", "allVideos", "producerReelId", "getById", "getByCategory", "getCategoriesInDisplayOrder", "getCategoriesWithCounts"]
    - path: "src/lib/data/index.ts"
      provides: "Public surface re-exporting 11 names (D-24)"
      exports: ["CATEGORIES", "categoryToSlug", "slugToCategory", "videos", "producerReelId", "getById", "getByCategory", "getCategoriesInDisplayOrder", "getCategoriesWithCounts", "Video", "Category"]
    - path: "src/lib/data/.videos-source-sha"
      provides: "3-line sidecar pinning _four commit SHA + videos.json sha256 + sync date (D-04 through D-08)"
      contains: "four_commit_sha="
    - path: "vite.config.ts"
      provides: "validateVideosPlugin() wired between tailwindcss() and sveltekit() in BOTH the top-level plugins array AND each Vitest project's plugins array (DATA-02 + data/ui project split)"
      contains: "validateVideosPlugin"
  key_links:
    - from: "vite.config.ts validateVideosPlugin()"
      to: "src/lib/data/videos.json"
      via: "buildStart hook: JSON.parse + VideoArraySchema.safeParse + (source,id) uniqueness loop"
      pattern: "VideoArraySchema.safeParse"
    - from: "src/lib/data/videos.ts"
      to: "src/lib/data/videos.json"
      via: "import rawVideos from './videos.json' + VideoArraySchema.parse(rawVideos)"
      pattern: "VideoArraySchema.parse\\(rawVideos\\)"
    - from: "src/lib/data/index.ts"
      to: "src/lib/data/videos.ts + schema.ts + categories.ts"
      via: "re-export of 11 names (D-24)"
      pattern: "export \\{ videos, producerReelId, getById"
---

<objective>
Mirror `_four`'s data layer byte-for-byte into `_three`: copy `videos.json` and the 4 loader files + 4 test files verbatim, wire the `validateVideosPlugin()` between `tailwindcss()` and `sveltekit()` in `vite.config.ts`, and create the `.videos-source-sha` sidecar that records the source commit SHA + content hash.

Purpose: Establishes DATA-01 (byte-identity), DATA-02 (Zod + Vite build-fail plugin), DATA-03 (drop-in `$lib/data` surface). Every downstream phase (3, 4, 5) imports from `$lib/data`; this plan is the foundation that makes those imports compile against the same shape as `_four`.

Output: 9 data-layer files (5 source + 4 test) mirrored verbatim, 1 sidecar created, `vite.config.ts` updated to call `validateVideosPlugin()` in both the top-level plugins array and the two Vitest projects (data + ui).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-layer/02-CONTEXT.md

<interfaces>
<!-- Public surface re-exported from $lib/data per D-24. Downstream phases bind against these 11 names. -->

From src/lib/data/index.ts (after this plan):
```typescript
export type { Video } from './schema';
export type { Category } from './categories';
export { CATEGORIES, categoryToSlug, slugToCategory } from './categories';
export {
  videos,
  producerReelId,
  getById,
  getByCategory,
  getCategoriesInDisplayOrder,
  getCategoriesWithCounts,
} from './videos';
```

From src/lib/data/schema.ts:
```typescript
export const CategorySchema: z.ZodEnum<typeof CATEGORIES>;
export const VideoSchema: z.ZodDiscriminatedUnion<'source', [...]>;
export const VideoArraySchema: z.ZodArray<typeof VideoSchema>;
export type Video = z.infer<typeof VideoSchema>;
```

From src/lib/data/videos.ts:
```typescript
export const videos: readonly Video[];        // 56 - hidden, currently 56
export const allVideos: readonly Video[];     // 56 - NOT re-exported via index.ts
export const producerReelId: '264677021';
export function getById(id: string): Video | undefined;
export function getByCategory(category: Category): readonly Video[];
export function getCategoriesInDisplayOrder(): readonly Category[];
export function getCategoriesWithCounts(): ReadonlyArray<{ category: Category; slug: string; count: number }>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mirror videos.json + 4 loader source files + 4 test files verbatim from _four</name>
  <files>
    src/lib/data/videos.json,
    src/lib/data/categories.ts,
    src/lib/data/schema.ts,
    src/lib/data/videos.ts,
    src/lib/data/index.ts,
    src/lib/data/categories.test.ts,
    src/lib/data/schema.test.ts,
    src/lib/data/videos.test.ts,
    src/lib/data/videos.json.test.ts
  </files>
  <read_first>
    - ../michelle_ngo_four/src/lib/data/videos.json (the byte-for-byte source — DO NOT re-author)
    - ../michelle_ngo_four/src/lib/data/categories.ts (D-21 verbatim mirror)
    - ../michelle_ngo_four/src/lib/data/schema.ts (D-21 verbatim mirror)
    - ../michelle_ngo_four/src/lib/data/videos.ts (D-21 verbatim mirror)
    - ../michelle_ngo_four/src/lib/data/index.ts (D-21 + D-24 — 11-name public surface)
    - ../michelle_ngo_four/src/lib/data/categories.test.ts (D-23 verbatim mirror)
    - ../michelle_ngo_four/src/lib/data/schema.test.ts (D-23 verbatim mirror)
    - ../michelle_ngo_four/src/lib/data/videos.test.ts (D-23 verbatim mirror)
    - ../michelle_ngo_four/src/lib/data/videos.json.test.ts (D-23 verbatim mirror)
  </read_first>
  <action>
    **Step A — Verify source files exist and prove byte-source SHA before copy:**
    Run these commands (PowerShell or bash — both supported):

    bash:
    ```bash
    cd "$(pwd)"  # _three repo root
    test -f ../michelle_ngo_four/src/lib/data/videos.json || { echo "ERROR: _four videos.json missing"; exit 1; }
    sha256sum ../michelle_ngo_four/src/lib/data/videos.json
    # Capture the 64-char hex output — it will appear again in the sidecar in Task 3.
    ```

    PowerShell equivalent (Windows):
    ```powershell
    Test-Path ../michelle_ngo_four/src/lib/data/videos.json
    Get-FileHash ../michelle_ngo_four/src/lib/data/videos.json -Algorithm SHA256
    ```

    **Step B — Copy the 9 files byte-for-byte (NO `_three`-specific edits, NO docstring rewrites, NO path-comment fixups unless a comment literally references a `_four`-only directory which none of these do):**

    bash:
    ```bash
    cp ../michelle_ngo_four/src/lib/data/videos.json src/lib/data/videos.json
    cp ../michelle_ngo_four/src/lib/data/categories.ts src/lib/data/categories.ts
    cp ../michelle_ngo_four/src/lib/data/schema.ts src/lib/data/schema.ts
    cp ../michelle_ngo_four/src/lib/data/videos.ts src/lib/data/videos.ts
    cp ../michelle_ngo_four/src/lib/data/index.ts src/lib/data/index.ts
    cp ../michelle_ngo_four/src/lib/data/categories.test.ts src/lib/data/categories.test.ts
    cp ../michelle_ngo_four/src/lib/data/schema.test.ts src/lib/data/schema.test.ts
    cp ../michelle_ngo_four/src/lib/data/videos.test.ts src/lib/data/videos.test.ts
    cp ../michelle_ngo_four/src/lib/data/videos.json.test.ts src/lib/data/videos.json.test.ts
    ```

    PowerShell equivalent:
    ```powershell
    Copy-Item ../michelle_ngo_four/src/lib/data/videos.json src/lib/data/videos.json
    Copy-Item ../michelle_ngo_four/src/lib/data/categories.ts src/lib/data/categories.ts
    Copy-Item ../michelle_ngo_four/src/lib/data/schema.ts src/lib/data/schema.ts
    Copy-Item ../michelle_ngo_four/src/lib/data/videos.ts src/lib/data/videos.ts
    Copy-Item ../michelle_ngo_four/src/lib/data/index.ts src/lib/data/index.ts
    Copy-Item ../michelle_ngo_four/src/lib/data/categories.test.ts src/lib/data/categories.test.ts
    Copy-Item ../michelle_ngo_four/src/lib/data/schema.test.ts src/lib/data/schema.test.ts
    Copy-Item ../michelle_ngo_four/src/lib/data/videos.test.ts src/lib/data/videos.test.ts
    Copy-Item ../michelle_ngo_four/src/lib/data/videos.json.test.ts src/lib/data/videos.json.test.ts
    ```

    **Step C — Verify byte-identity on videos.json (the load-bearing one):**
    bash:
    ```bash
    cmp -s ../michelle_ngo_four/src/lib/data/videos.json src/lib/data/videos.json && echo "videos.json byte-identical" || { echo "DRIFT detected after copy"; exit 1; }
    ```

    PowerShell equivalent:
    ```powershell
    $a = Get-FileHash ../michelle_ngo_four/src/lib/data/videos.json -Algorithm SHA256
    $b = Get-FileHash src/lib/data/videos.json -Algorithm SHA256
    if ($a.Hash -ne $b.Hash) { Write-Error "DRIFT detected after copy"; exit 1 } else { Write-Host "videos.json byte-identical" }
    ```

    **Per D-21 and D-23:** Do NOT edit the copied files. Docstrings that reference `_four`'s D-numbers stay (they are correct historical provenance, not stale). Comments referencing "Wave 0 used lazy `await import(...)`" in the test files stay verbatim — they are the inherited test ergonomics.

    **Decision references:** D-01 (plain cp), D-21 (verbatim mirror), D-22 (zero `_three`-specific helpers), D-23 (mirror all 4 test files), D-24 (11-name public surface).
  </action>
  <verify>
    <automated>
      bash one-liner (any shell):
      ```bash
      # All 9 files exist
      for f in src/lib/data/videos.json src/lib/data/categories.ts src/lib/data/schema.ts src/lib/data/videos.ts src/lib/data/index.ts src/lib/data/categories.test.ts src/lib/data/schema.test.ts src/lib/data/videos.test.ts src/lib/data/videos.json.test.ts; do test -f "$f" || { echo "MISSING: $f"; exit 1; }; done

      # videos.json byte-identical
      cmp -s ../michelle_ngo_four/src/lib/data/videos.json src/lib/data/videos.json && echo OK

      # index.ts has all 11 names
      grep -c 'CATEGORIES\|categoryToSlug\|slugToCategory\|videos\|producerReelId\|getById\|getByCategory\|getCategoriesInDisplayOrder\|getCategoriesWithCounts\|Video\|Category' src/lib/data/index.ts

      # schema.ts contains the load-bearing Zod 4 idioms
      grep -q "z.discriminatedUnion('source'" src/lib/data/schema.ts
      grep -q "z.strictObject" src/lib/data/schema.ts
      grep -q "z.iso.date()" src/lib/data/schema.ts

      # videos.ts has producerReelId = '264677021'
      grep -q "producerReelId = '264677021'" src/lib/data/videos.ts
      ```
    </automated>
  </verify>
  <acceptance_criteria>
    - All 9 listed files exist at exactly the paths in `<files>` above.
    - `sha256sum src/lib/data/videos.json` equals `sha256sum ../michelle_ngo_four/src/lib/data/videos.json` (or `cmp -s` returns exit 0; PowerShell `Get-FileHash` Hash values are equal).
    - `grep -c "export" src/lib/data/index.ts` shows at least 3 export statements (1 `export type { Video }`, 1 `export type { Category }`, 1 `export { CATEGORIES, categoryToSlug, slugToCategory }`, 1 `export { videos, producerReelId, ... }` — 4 export statements, 11 names).
    - `grep "z.discriminatedUnion('source'" src/lib/data/schema.ts` returns at least 1 match.
    - `grep "z.strictObject" src/lib/data/schema.ts` returns at least 2 matches (one per source branch).
    - `grep "z.iso.date()" src/lib/data/schema.ts` returns at least 1 match.
    - `grep "producerReelId = '264677021'" src/lib/data/videos.ts` returns 1 match.
    - `grep "VideoArraySchema.parse(rawVideos)" src/lib/data/videos.ts` returns 1 match.
    - `grep "CATEGORIES" src/lib/data/categories.ts` returns at least 2 matches (one const declaration, one type derivation).
    - No new files exist beyond the 9 listed (no _three-specific helpers per D-22).
  </acceptance_criteria>
  <done>
    9 files mirrored byte-for-byte from `_four`. `videos.json` sha256 matches sibling. The 11 public exports are present in `index.ts`. No `_three`-specific edits introduced.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire validateVideosPlugin into vite.config.ts (top-level + both Vitest projects) and adopt the data/ui project split</name>
  <files>vite.config.ts</files>
  <read_first>
    - vite.config.ts (existing _three vite config — current state has plugins: [tailwindcss(), sveltekit()] and a single jsdom test config)
    - ../michelle_ngo_four/vite.config.ts (the source of truth for the validateVideosPlugin block AND the data/ui project split)
    - src/lib/data/schema.ts (just-mirrored in Task 1 — provides VideoArraySchema import target)
    - vitest-setup-ui.ts (referenced by the ui project setupFiles array)
  </read_first>
  <action>
    Replace the entire contents of `vite.config.ts` with the block below. This block:
    1. Adds the `validateVideosPlugin()` function definition (mirrored verbatim from `_four/vite.config.ts` lines 33-65).
    2. Wires it into the top-level `plugins:` array between `tailwindcss()` and `sveltekit()` per the existing comment in `_three/vite.config.ts`.
    3. Replaces the single jsdom `test:` block with the two-project split (data=node, ui=jsdom) — required so the new `src/lib/data/**/*.test.ts` files run in a Node env (faster, no jsdom bootstrap) while existing component/route tests stay in jsdom.
    4. Each project re-declares the plugin set (per the `_four` comment: "required for $lib/* alias resolution and Svelte component compilation").
    5. Adds the `resolve: { conditions: ['browser'] }` block on the `ui` project only (not at top level — the existing top-level browser conditions in `_three`'s current vite.config.ts is migrated INTO the ui project where Svelte's `mount()` actually needs it).

    **Exact file contents to write (full replacement — do not merge by hand):**

    ```typescript
    /// <reference types="vitest/config" />
    import { sveltekit } from '@sveltejs/kit/vite';
    import { defineConfig, type Plugin } from 'vite';
    import tailwindcss from '@tailwindcss/vite';
    import { z } from 'zod';
    import { readFileSync } from 'node:fs';
    import { resolve, dirname } from 'node:path';
    import { fileURLToPath } from 'node:url';
    import { VideoArraySchema } from './src/lib/data/schema';

    const __dirname = dirname(fileURLToPath(import.meta.url));

    /**
     * DATA-02: Fail `pnpm build` (and `pnpm dev` server start) on a schema violation
     * in src/lib/data/videos.json. Runs in `buildStart`, the canonical Rollup-compatible
     * lifecycle hook for fail-fast pre-bundle validation.
     *
     * Mirrored verbatim from ../michelle_ngo_four/vite.config.ts (D-21 / DATA-02).
     */
    function validateVideosPlugin(): Plugin {
      return {
        name: 'validate-videos',
        buildStart() {
          const path = resolve(__dirname, 'src/lib/data/videos.json');
          let raw: unknown;
          try {
            raw = JSON.parse(readFileSync(path, 'utf-8'));
          } catch (e) {
            this.error(`videos.json is not valid JSON: ${(e as Error).message}`);
            return; // unreachable — this.error throws
          }

          const result = VideoArraySchema.safeParse(raw);
          if (!result.success) {
            const pretty = z.prettifyError(result.error);
            this.error(`videos.json failed schema validation:\n${pretty}`);
            return; // unreachable
          }

          // Cross-row constraint: (source, id) must be unique across all records.
          const seen = new Set<string>();
          for (const v of result.data) {
            const key = `${v.source}:${v.id}`;
            if (seen.has(key)) {
              this.error(`videos.json: duplicate (source, id) pair: ${key}`);
              return; // unreachable
            }
            seen.add(key);
          }
        },
      };
    }

    export default defineConfig({
      // Plugin order matters: tailwindcss BEFORE sveltekit (Phase 1 Pattern 1);
      // validateVideosPlugin sits immediately before sveltekit() so the validation
      // failure aborts the build BEFORE Svelte starts compiling routes that import
      // the data. (Mirrors _four's plugin order.)
      plugins: [tailwindcss(), validateVideosPlugin(), sveltekit()],
      test: {
        // Phase 2 D-21 / CONTEXT Established Patterns: Vitest two-project split
        // (data=node, ui=jsdom). Each project re-declares the plugin set so
        // SvelteKit Vite plugins (tailwindcss, validateVideosPlugin, sveltekit)
        // load in BOTH projects — required for $lib/* alias resolution and
        // Svelte component compilation.
        projects: [
          {
            plugins: [tailwindcss(), validateVideosPlugin(), sveltekit()],
            test: {
              name: 'data',
              include: ['src/lib/data/**/*.{test,spec}.{js,ts}'],
              environment: 'node',
              globals: false,
            },
          },
          {
            plugins: [tailwindcss(), validateVideosPlugin(), sveltekit()],
            // `mount()` from 'svelte' resolves to svelte/src/index-server.js
            // unless we tell Vite to use browser conditions. Without this, every
            // ui test crashes with `lifecycle_function_unavailable: mount(...)
            // is not available on the server`.
            resolve: {
              conditions: ['browser'],
            },
            test: {
              name: 'ui',
              include: [
                'src/lib/components/**/*.{test,spec}.{js,ts}',
                'src/lib/**/*.{test,spec}.{js,ts}',
                'src/routes/**/*.{test,spec}.{js,ts}',
              ],
              exclude: ['src/lib/data/**'],
              environment: 'jsdom',
              globals: false,
              setupFiles: ['./vitest-setup-ui.ts'],
            },
          },
        ],
        coverage: {
          provider: 'v8',
          reporter: ['text', 'html'],
          include: ['src/lib/data/**/*.ts'],
          exclude: ['src/lib/data/**/*.test.ts'],
        },
      },
    });
    ```

    **Note on the ui project `include` / `exclude`:** Existing Phase 1 tests live under `src/lib/storage.test.ts`, `src/lib/intersectionVisibility.test.ts`, and `src/routes/smoke-page.test.ts` (per STATE.md). The `include` array MUST capture all of them while `exclude: ['src/lib/data/**']` keeps the new data tests OUT of the ui project (they run in the `data` project's node env instead). If executor finds during smoke-test that a Phase 1 test path doesn't match the `include` globs, widen them (Claude's discretion per D-21 §"Any minor vitest.config adjustments needed if _four's test file paths or $lib alias resolution differ slightly in _three's setup").

    **Decision references:** D-21 (verbatim plugin block), DATA-02 (build-fail behavior), CONTEXT Established Patterns §Vitest two-project split.
  </action>
  <verify>
    <automated>
      ```bash
      # vite.config.ts wires the plugin in the top-level plugins array
      grep -q "plugins: \[tailwindcss(), validateVideosPlugin(), sveltekit()\]" vite.config.ts

      # validateVideosPlugin function is defined
      grep -q "function validateVideosPlugin(): Plugin" vite.config.ts

      # Two-project split is present
      grep -q "name: 'data'" vite.config.ts
      grep -q "name: 'ui'" vite.config.ts

      # data project uses node env
      grep -A 5 "name: 'data'" vite.config.ts | grep -q "environment: 'node'"

      # ui project excludes data tests
      grep -q "exclude: \['src/lib/data/\*\*'\]" vite.config.ts

      # Imports are correct
      grep -q "import { VideoArraySchema } from './src/lib/data/schema'" vite.config.ts
      grep -q "import { z } from 'zod'" vite.config.ts

      # pnpm check passes (no type errors after the rewrite)
      pnpm check

      # pnpm build succeeds with valid videos.json (validates the plugin doesn't false-positive)
      pnpm build

      # pnpm test passes for the data project (proves data tests run in node env)
      pnpm test
      ```
    </automated>
  </verify>
  <acceptance_criteria>
    - `vite.config.ts` contains the exact string `plugins: [tailwindcss(), validateVideosPlugin(), sveltekit()]` (top-level array, plugin ordering preserved).
    - `vite.config.ts` defines `function validateVideosPlugin(): Plugin` returning a Rollup-compatible plugin with `name: 'validate-videos'` and a `buildStart()` hook.
    - The `buildStart()` hook calls `VideoArraySchema.safeParse(raw)` AND has a `for...of result.data` loop checking `${v.source}:${v.id}` Set uniqueness with `this.error("...duplicate (source, id) pair...")` on collision.
    - `vite.config.ts` exports a `test.projects` array with exactly 2 entries: one named `'data'` (env `'node'`, include `src/lib/data/**/*.{test,spec}.{js,ts}`) and one named `'ui'` (env `'jsdom'`, include includes `src/routes/**` and `src/lib/**`, exclude `src/lib/data/**`).
    - The `ui` project has `resolve: { conditions: ['browser'] }`.
    - `pnpm check` exits 0 (no TS errors).
    - `pnpm build` exits 0 (no schema validation failure on valid videos.json — proves the plugin runs but does not false-positive).
    - `pnpm test` exits 0 with both projects' tests reported (data project's 4 new test files + ui project's existing Phase 1 tests).
  </acceptance_criteria>
  <done>
    `vite.config.ts` wires `validateVideosPlugin()` in the top-level plugins array between `tailwindcss()` and `sveltekit()`, AND in each of the 2 Vitest projects' plugin arrays. `pnpm check`, `pnpm test`, and `pnpm build` all pass cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create .videos-source-sha sidecar + smoke-test the build-fail behavior</name>
  <files>src/lib/data/.videos-source-sha</files>
  <read_first>
    - src/lib/data/videos.json (just mirrored in Task 1 — needed to compute sha256)
    - ../michelle_ngo_four/.git/HEAD (or run `git -C ../michelle_ngo_four rev-parse HEAD` to read the SHA — D-05: pull from _four's current main HEAD at sync time)
    - .planning/phases/02-data-layer/02-CONTEXT.md §D-06 (sidecar exact 3-line format)
  </read_first>
  <action>
    **Step A — Compute the three values for the sidecar:**

    ```bash
    # 1) _four's current main HEAD SHA (D-05 — pin at sync time)
    FOUR_SHA=$(git -C ../michelle_ngo_four rev-parse HEAD)
    echo "four_commit_sha=$FOUR_SHA"

    # 2) sha256 of _three's just-copied videos.json (D-06)
    VIDEOS_SHA=$(sha256sum src/lib/data/videos.json | awk '{print $1}')
    echo "videos_json_sha256=$VIDEOS_SHA"

    # 3) Today's date in ISO 8601 YYYY-MM-DD
    SYNCED_AT=$(date -u +%Y-%m-%d)
    echo "synced_at=$SYNCED_AT"
    ```

    PowerShell equivalent:
    ```powershell
    $FOUR_SHA = (git -C ../michelle_ngo_four rev-parse HEAD).Trim()
    $VIDEOS_SHA = (Get-FileHash src/lib/data/videos.json -Algorithm SHA256).Hash.ToLower()
    $SYNCED_AT = (Get-Date -Format 'yyyy-MM-dd')
    "four_commit_sha=$FOUR_SHA`nvideos_json_sha256=$VIDEOS_SHA`nsynced_at=$SYNCED_AT" | Out-File -Encoding ASCII -NoNewline src/lib/data/.videos-source-sha
    ```

    **Step B — Write `src/lib/data/.videos-source-sha` with EXACTLY this 3-line plain-text format (D-06):**

    ```
    four_commit_sha=<40-char hex from Step A.1>
    videos_json_sha256=<64-char hex from Step A.2>
    synced_at=<ISO date from Step A.3>
    ```

    No JSON, no YAML, no comments, no header line — three lines of `key=value` (D-06 specifies format). Trailing newline at end of file is fine; sidecar parsing in Plan 02-02 will tolerate it.

    Use Write tool to create the file with the literal computed values substituted in. Example (the executor's actual values will differ — these are illustrative only):

    ```
    four_commit_sha=07667658ee2fd16a3d56b66bbe832d08fc3badd5
    videos_json_sha256=<computed at runtime — do NOT hardcode this in the plan>
    synced_at=2026-05-25
    ```

    **Step C — Negative smoke-test the build-fail plugin (DATA-02 verification — required, not optional):**

    Prove `validateVideosPlugin()` actually aborts `pnpm build` when the data is broken. This is the load-bearing assertion for DATA-02 ("intentionally breaking a record fails the build").

    Procedure:
    1. Copy `src/lib/data/videos.json` to `src/lib/data/videos.json.bak` (backup).
    2. Open `src/lib/data/videos.json`, find the first video record, and corrupt it ONE of the following ways:
       - Change its `category` field to a string NOT in `CATEGORIES` (e.g., `"InvalidCategory"`).
       - OR delete its `title` field.
       - OR change its `source` field to `"tiktok"`.
    3. Run `pnpm build` and CAPTURE the exit code + stderr.
    4. Verify: exit code is non-zero AND stderr contains the literal string `videos.json failed schema validation` (or `videos.json: duplicate (source, id) pair:` if the executor chose the duplication corruption instead).
    5. Restore from backup: `cp src/lib/data/videos.json.bak src/lib/data/videos.json && rm src/lib/data/videos.json.bak` (or PowerShell `Move-Item -Force src/lib/data/videos.json.bak src/lib/data/videos.json`).
    6. Re-verify byte-identity post-restore: `cmp -s ../michelle_ngo_four/src/lib/data/videos.json src/lib/data/videos.json` returns exit 0.
    7. Re-run `pnpm build` and confirm it now succeeds.

    Document the smoke-test outcome in the plan summary's `notes` field (which corruption was used, what error message appeared, exit code observed). Do NOT commit the broken `videos.json` or the backup file.

    **Decision references:** D-04 / D-05 / D-06 (sidecar exact format), DATA-02 (build-fail behavior — smoke-test mandatory).
  </action>
  <verify>
    <automated>
      ```bash
      # Sidecar exists at the correct path
      test -f src/lib/data/.videos-source-sha

      # Three lines, three keys, in the exact D-06 order
      grep -q "^four_commit_sha=[a-f0-9]\{40\}$" src/lib/data/.videos-source-sha
      grep -q "^videos_json_sha256=[a-f0-9]\{64\}$" src/lib/data/.videos-source-sha
      grep -q "^synced_at=[0-9]\{4\}-[0-9]\{2\}-[0-9]\{2\}$" src/lib/data/.videos-source-sha

      # Total non-empty lines = 3
      grep -cv "^$" src/lib/data/.videos-source-sha
      # Expected: 3

      # The recorded sha256 matches the actual file sha256
      RECORDED=$(grep "^videos_json_sha256=" src/lib/data/.videos-source-sha | cut -d= -f2)
      ACTUAL=$(sha256sum src/lib/data/videos.json | awk '{print $1}')
      [ "$RECORDED" = "$ACTUAL" ] && echo "sidecar sha matches" || { echo "DRIFT: sidecar sha != file sha"; exit 1; }

      # The recorded _four SHA is reachable in _four's git
      RECORDED_FOUR=$(grep "^four_commit_sha=" src/lib/data/.videos-source-sha | cut -d= -f2)
      git -C ../michelle_ngo_four cat-file -e "$RECORDED_FOUR" && echo "_four SHA reachable" || { echo "ERROR: pinned SHA not in _four"; exit 1; }

      # Final build still passes (post-restore)
      pnpm build
      ```
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/data/.videos-source-sha` exists at exactly this path.
    - File contains exactly 3 non-empty lines.
    - Line 1 matches regex `^four_commit_sha=[a-f0-9]{40}$` (40-char lowercase hex).
    - Line 2 matches regex `^videos_json_sha256=[a-f0-9]{64}$` (64-char lowercase hex).
    - Line 3 matches regex `^synced_at=\d{4}-\d{2}-\d{2}$` (ISO date YYYY-MM-DD).
    - The `videos_json_sha256` value equals the actual `sha256sum src/lib/data/videos.json` output (proves sidecar is consistent with the file it documents).
    - The `four_commit_sha` value is reachable in `../michelle_ngo_four`'s git history (`git -C ../michelle_ngo_four cat-file -e <sha>` exits 0).
    - Smoke-test executed: a corrupted `videos.json` produced a non-zero `pnpm build` exit AND stderr contained `videos.json failed schema validation` OR `videos.json: duplicate (source, id) pair:`.
    - Post-restore: `cmp -s ../michelle_ngo_four/src/lib/data/videos.json src/lib/data/videos.json` exits 0; `pnpm build` exits 0.
    - No `videos.json.bak` file remains in the working tree.
  </acceptance_criteria>
  <done>
    Sidecar created with valid 3-line format. Sidecar's recorded sha256 matches the file. Sidecar's recorded `four_commit_sha` is a reachable commit in `_four`. Build-fail smoke-test confirmed (`pnpm build` aborts non-zero on corrupted JSON, succeeds on restored JSON).
  </done>
</task>

</tasks>

<verification>
**Plan-level verification (run after all 3 tasks):**

```bash
# 1. videos.json byte-identical to _four
cmp -s ../michelle_ngo_four/src/lib/data/videos.json src/lib/data/videos.json && echo "DATA-01 OK"

# 2. All 11 public exports present in $lib/data
grep -E "^export (type )?\{" src/lib/data/index.ts

# 3. validateVideosPlugin wired
grep -q "validateVideosPlugin()" vite.config.ts && echo "DATA-02 wired"

# 4. Sidecar valid
grep -q "^four_commit_sha=[a-f0-9]\{40\}$" src/lib/data/.videos-source-sha

# 5. Full toolchain green
pnpm check && pnpm test && pnpm build
```
</verification>

<success_criteria>
- **DATA-01:** `videos.json` byte-identical to `_four`'s (`cmp -s` exits 0; sha256 sums match).
- **DATA-02:** `pnpm build` fails non-zero with a Zod prettyError message when a record is intentionally broken; restoring the file lets the build succeed again.
- **DATA-03:** All 11 public exports in `src/lib/data/index.ts` (verified by grep); `pnpm check` passes (TS strict + `noUncheckedIndexedAccess` honored).
- All 4 test files pass: `pnpm test` exits 0 with the data project's tests reported separately from the ui project's.
- Sidecar `src/lib/data/.videos-source-sha` exists with the exact 3-line format and the recorded sha256 matches the file.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-layer/02-01-SUMMARY.md` recording:
- The exact `four_commit_sha` pinned (40-char hex).
- The exact `videos_json_sha256` (64-char hex).
- The exact `synced_at` date.
- Which corruption was used in the smoke-test (category? title? source? duplicate?) and the literal error string observed in stderr.
- The set of test files that ran in the `data` vs `ui` project (count + paths from `pnpm test` output).
- Any unexpected adjustments made to the `ui` project's `include` globs (per D-21 Claude's-discretion clause).
</output>
