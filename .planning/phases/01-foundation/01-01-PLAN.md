---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - .npmrc
  - .nvmrc
  - .gitignore
  - .prettierrc
  - .prettierignore
  - eslint.config.js
  - svelte.config.js
  - tsconfig.json
  - vite.config.ts
  - .husky/pre-commit
  - src/app.html
  - src/routes/+layout.svelte
  - src/routes/+layout.ts
  - src/routes/+page.svelte
  - src/app.css
  - src/lib/.gitkeep
  - static/robots.txt
  - README.md
autonomous: true
requirements:
  - FOUND-01
must_haves:
  truths:
    - "Fresh clone followed by `pnpm install` and `pnpm build` succeeds with exit code 0"
    - "TypeScript strict mode (with noUncheckedIndexedAccess and noImplicitOverride) is active and `pnpm check` exits 0"
    - "Tailwind v4 utilities resolve at build time (Vite plugin order: tailwindcss → sveltekit)"
    - "Svelte 5 runes mode is active (svelte 5.55.5 from package.json) with no warnings"
    - "All 5 cinematic-layer deps (runed, @sveltejs/enhanced-img, @tailwindcss/typography, @playwright/test, @axe-core/playwright, @testing-library/svelte, @testing-library/jest-dom) are present in devDependencies"
    - "`adapter-static` produces a `build/` directory containing `index.html` and a `_app/` asset bundle"
  artifacts:
    - path: "package.json"
      provides: "All locked deps from _four + 5 cinematic-layer additions + scripts"
      contains: "@sveltejs/kit"
    - path: "svelte.config.js"
      provides: "adapter-static config + paths.base from BASE_PATH env"
      contains: "paths"
    - path: "tsconfig.json"
      provides: "TS strict + noUncheckedIndexedAccess + noImplicitOverride"
      contains: "noUncheckedIndexedAccess"
    - path: "vite.config.ts"
      provides: "tailwindcss() plugin before sveltekit() in plugins array"
      contains: "tailwindcss()"
    - path: "src/app.html"
      provides: "SvelteKit shell with viewport meta"
      contains: "%sveltekit.body%"
    - path: "src/routes/+layout.svelte"
      provides: "Root layout importing app.css with noindex meta"
      contains: "../app.css"
    - path: "src/routes/+page.svelte"
      provides: "Smoke route at /"
      min_lines: 1
    - path: "src/app.css"
      provides: "Tailwind v4 import (theme tokens land in plan 02)"
      contains: "@import \"tailwindcss\""
    - path: "eslint.config.js"
      provides: "Flat ESLint config matching _four"
      contains: "tseslint.config"
    - path: ".prettierrc"
      provides: "Prettier config matching _four byte-for-byte (printWidth 100, singleQuote, semis)"
      contains: "prettier-plugin-svelte"
  key_links:
    - from: "vite.config.ts"
      to: "tailwindcss + sveltekit"
      via: "plugins array order (tailwindcss BEFORE sveltekit)"
      pattern: "plugins:\\s*\\[\\s*tailwindcss\\(\\)"
    - from: "svelte.config.js"
      to: "process.env.BASE_PATH"
      via: "paths.base assignment"
      pattern: "base:\\s*process\\.env\\.BASE_PATH"
    - from: "package.json"
      to: "pnpm@11.0.9"
      via: "packageManager field"
      pattern: "\"packageManager\":\\s*\"pnpm@11\\.0\\.9\""
    - from: ".husky/pre-commit"
      to: "lint-staged"
      via: "shell hook"
      pattern: "pnpm lint-staged"
---

<objective>
Scaffold a buildable SvelteKit 2 + Svelte 5 + TypeScript strict + Tailwind v4 project that mirrors `../michelle_ngo_four/`'s tooling byte-for-byte AND installs the 5 cinematic-layer dep additions specified in CONTEXT.md so they are not retrofitted later.

Purpose: Satisfy FOUND-01 (clean static build, TS strict, Svelte 5 runes, Tailwind v4) and lock the version floor + plugin order before any feature code lands. Without this scaffold, plans 01-02 and 01-03 have nothing to attach `@theme` tokens or CI workflows to.

Output: A fresh project tree where `pnpm install && pnpm build && pnpm check` all exit 0 on a clean clone, with every dep from the sibling's package.json + the 5 new cinematic-layer deps installed at the exact versions called out in CLAUDE.md "Technology Stack".
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- These exact files MUST be mirrored from the sibling project. Treat each as the source of truth. -->
<!-- Sibling project root: ../michelle_ngo_four/ -->

Sibling package.json devDependencies (mirror EVERY version exactly; DO NOT bump):
```json
{
  "@eslint/js": "9.39.4",
  "@sveltejs/adapter-static": "3.0.10",
  "@sveltejs/kit": "2.59.1",
  "@sveltejs/vite-plugin-svelte": "7.1.2",
  "@tailwindcss/vite": "4.3.0",
  "@types/node": "22.19.18",
  "@vitest/coverage-v8": "4.1.5",
  "eslint": "9.39.4",
  "eslint-plugin-svelte": "3.17.1",
  "globals": "17.6.0",
  "husky": "9.1.7",
  "jsdom": "29.1.1",
  "lint-staged": "17.0.4",
  "prettier": "3.8.3",
  "prettier-plugin-svelte": "3.5.1",
  "svelte": "5.55.5",
  "svelte-check": "4.4.6",
  "tailwindcss": "4.3.0",
  "typescript": "5.9.3",
  "typescript-eslint": "8.59.2",
  "vite": "8.0.7",
  "vitest": "4.1.5",
  "zod": "4.4.3"
}
```

Cinematic-layer ADDITIONS (NEW for `_three`, NOT in sibling) — pin to these exact versions:
```json
{
  "runed": "0.37.1",
  "@sveltejs/enhanced-img": "0.10.4",
  "@tailwindcss/typography": "0.5.19",
  "@playwright/test": "1.60.0",
  "@axe-core/playwright": "4.11.4",
  "@testing-library/svelte": "5.3.1",
  "@testing-library/jest-dom": "6.9.1"
}
```

Sibling svelte.config.js (mirror verbatim — 21 lines):
```js
import adapter from '@sveltejs/adapter-static';
import { vitePreprocess } from '@sveltejs/vite-plugin-svelte';

/** @type {import('@sveltejs/kit').Config} */
const config = {
  preprocess: vitePreprocess(),
  kit: {
    adapter: adapter({
      pages: 'build',
      assets: 'build',
      fallback: '404.html',
      precompress: false,
      strict: true,
    }),
    paths: {
      base: process.env.BASE_PATH ?? '',
    },
  },
};

export default config;
```

Sibling tsconfig.json (mirror verbatim — 8 lines):
```json
{
  "extends": "./.svelte-kit/tsconfig.json",
  "compilerOptions": {
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true
  }
}
```

Sibling eslint.config.js (mirror verbatim — 32 lines):
```js
import js from '@eslint/js';
import svelte from 'eslint-plugin-svelte';
import tseslint from 'typescript-eslint';
import globals from 'globals';

export default tseslint.config(
  js.configs.recommended,
  ...tseslint.configs.recommended,
  ...svelte.configs.recommended,
  {
    languageOptions: {
      globals: {
        ...globals.browser,
        ...globals.node,
      },
    },
  },
  {
    files: ['**/*.svelte', '**/*.svelte.ts', '**/*.svelte.js'],
    languageOptions: {
      parserOptions: {
        parser: tseslint.parser,
        projectService: true,
        extraFileExtensions: ['.svelte'],
      },
    },
  },
  {
    ignores: ['build/', '.svelte-kit/', 'dist/', 'node_modules/'],
  }
);
```

Sibling .prettierrc (mirror verbatim):
```json
{
  "semi": true,
  "singleQuote": true,
  "printWidth": 100,
  "trailingComma": "es5",
  "plugins": ["prettier-plugin-svelte"],
  "overrides": [
    { "files": "*.svelte", "options": { "parser": "svelte" } },
    { "files": "*.css", "options": { "singleQuote": false } }
  ]
}
```

Sibling .prettierignore (mirror verbatim, dropping the videos.json line — that's Phase 2):
```
.svelte-kit
build
node_modules
pnpm-lock.yaml
package-lock.json
yarn.lock
.planning
_prep
```

Sibling .npmrc:
```
engine-strict=true
```

Sibling .nvmrc:
```
22
```

Sibling .husky/pre-commit (mirror verbatim, single line):
```
pnpm lint-staged
```

Sibling vite.config.ts plugin-order pattern (the validateVideosPlugin is PHASE 2 — DO NOT include in this plan):
```ts
import { sveltekit } from '@sveltejs/kit/vite';
import { defineConfig } from 'vite';
import tailwindcss from '@tailwindcss/vite';

export default defineConfig({
  plugins: [tailwindcss(), sveltekit()],
});
```

Sibling src/app.html (mirror verbatim — 13 lines):
```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" href="%sveltekit.assets%/favicon.png" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    %sveltekit.head%
  </head>
  <body data-sveltekit-preload-data="hover">
    <div style="display: contents">%sveltekit.body%</div>
  </body>
</html>
```

Sibling static/robots.txt (mirror verbatim — staging noindex; Phase 7 flips):
```
User-agent: *
Disallow: /
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create package.json, lockfile-controlling config, and root tooling files</name>
  <files>package.json, pnpm-workspace.yaml, .npmrc, .nvmrc, .gitignore, .prettierrc, .prettierignore, eslint.config.js, README.md</files>
  <read_first>
    - C:\Users\Mkaru\Documents\Hello_World\hugginface_profile\Websites\michelle_ngo_three\CLAUDE.md (to confirm version pins under "Technology Stack")
    - C:\Users\Mkaru\Documents\Hello_World\hugginface_profile\Websites\michelle_ngo_three\.planning\phases\01-foundation\01-CONTEXT.md (D-13 fonts mention, Claude's discretion on README)
    - C:\Users\Mkaru\Documents\Hello_World\hugginface_profile\Websites\michelle_ngo_four\package.json
    - C:\Users\Mkaru\Documents\Hello_World\hugginface_profile\Websites\michelle_ngo_four\.prettierrc
    - C:\Users\Mkaru\Documents\Hello_World\hugginface_profile\Websites\michelle_ngo_four\.prettierignore
    - C:\Users\Mkaru\Documents\Hello_World\hugginface_profile\Websites\michelle_ngo_four\eslint.config.js
    - C:\Users\Mkaru\Documents\Hello_World\hugginface_profile\Websites\michelle_ngo_four\.npmrc
    - C:\Users\Mkaru\Documents\Hello_North\hugginface_profile\Websites\michelle_ngo_four\.nvmrc (the existing file — content is literally "22")
    - C:\Users\Mkaru\Documents\Hello_World\hugginface_profile\Websites\michelle_ngo_four\.gitignore (if present; otherwise use the standard SvelteKit ignore set below)
  </read_first>
  <action>
    Create the root tooling files exactly as specified below (this implements D-09..D-17 prerequisites — the build floor — and Claude's-Discretion items "husky/lint-staged version pins use _four's package.json for parity" and "match `.prettierrc` specifics to _four exactly").

    1. Write `package.json` with these EXACT contents (name swapped to `michelle-ngo-three`, plus the 5 cinematic-layer additions appended alphabetically into devDependencies):
       ```json
       {
         "name": "michelle-ngo-three",
         "private": true,
         "version": "0.0.1",
         "type": "module",
         "packageManager": "pnpm@11.0.9",
         "engines": { "node": ">=22" },
         "scripts": {
           "dev": "vite dev",
           "build": "vite build",
           "preview": "vite preview",
           "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
           "check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
           "test": "vitest run --passWithNoTests",
           "test:watch": "vitest",
           "test:e2e": "playwright test",
           "lint": "eslint .",
           "format": "prettier --write .",
           "prepare": "husky"
         },
         "lint-staged": {
           "*.{js,ts,svelte}": ["eslint --fix", "prettier --write"],
           "*.{css,json,md,yaml,yml}": ["prettier --write"]
         },
         "devDependencies": {
           "@axe-core/playwright": "4.11.4",
           "@eslint/js": "9.39.4",
           "@playwright/test": "1.60.0",
           "@sveltejs/adapter-static": "3.0.10",
           "@sveltejs/enhanced-img": "0.10.4",
           "@sveltejs/kit": "2.59.1",
           "@sveltejs/vite-plugin-svelte": "7.1.2",
           "@tailwindcss/typography": "0.5.19",
           "@tailwindcss/vite": "4.3.0",
           "@testing-library/jest-dom": "6.9.1",
           "@testing-library/svelte": "5.3.1",
           "@types/node": "22.19.18",
           "@vitest/coverage-v8": "4.1.5",
           "eslint": "9.39.4",
           "eslint-plugin-svelte": "3.17.1",
           "globals": "17.6.0",
           "husky": "9.1.7",
           "jsdom": "29.1.1",
           "lint-staged": "17.0.4",
           "prettier": "3.8.3",
           "prettier-plugin-svelte": "3.5.1",
           "runed": "0.37.1",
           "svelte": "5.55.5",
           "svelte-check": "4.4.6",
           "tailwindcss": "4.3.0",
           "typescript": "5.9.3",
           "typescript-eslint": "8.59.2",
           "vite": "8.0.7",
           "vitest": "4.1.5",
           "zod": "4.4.3"
         }
       }
       ```
       Note: drop sibling's `test:data` and `test:build-fails` and `test:prerender` scripts — they're Phase 2 concerns. Add `test:e2e` because Plan 01-03 needs it.

    2. Write `pnpm-workspace.yaml` with empty packages list (matches sibling — used to suppress monorepo-confusion warnings on Windows):
       ```yaml
       packages: []
       ```

    3. Write `.npmrc` (single line): `engine-strict=true`

    4. Write `.nvmrc` (single line, no trailing newline): `22`

    5. Write `.gitignore` (standard SvelteKit ignore set):
       ```
       node_modules
       /build
       /.svelte-kit
       /package
       .env
       .env.*
       !.env.example
       !.env.test
       vite.config.js.timestamp-*
       vite.config.ts.timestamp-*

       # OS
       .DS_Store
       Thumbs.db

       # Playwright (added Phase 1 plan 03)
       /test-results/
       /playwright-report/
       /blob-report/
       /playwright/.cache/
       ```

    6. Write `.prettierrc` byte-identical to the interface block above (sibling's content).

    7. Write `.prettierignore` with this content (sibling's content MINUS the videos.json line — that's Phase 2):
       ```
       .svelte-kit
       build
       node_modules
       pnpm-lock.yaml
       package-lock.json
       yarn.lock
       .planning
       _prep
       ```

    8. Write `eslint.config.js` byte-identical to the interface block above.

    9. Write `README.md` (Claude's-Discretion shape — minimal, point to sibling):
       ```markdown
       # Michelle Ngo Portfolio — Cinematic Cut (`_three`)

       Cinematic-immersive A/B sibling of [`michelle_ngo_four`](../michelle_ngo_four), the editorial-modern v1. Same 56 videos, same SvelteKit 2 + Svelte 5 + TypeScript strict + Tailwind v4 stack — different visual language: dark full-bleed, scroll-snapped fullscreen reels with silent muted preview loops.

       See `.planning/PROJECT.md` for the full project brief and `.planning/ROADMAP.md` for the 7-phase delivery plan.

       ## Local development

       ```bash
       pnpm install
       pnpm dev       # local dev server
       pnpm build     # static build into ./build
       pnpm check     # svelte-check (type-check)
       pnpm lint      # eslint
       pnpm format    # prettier --write
       pnpm test      # vitest (unit + component)
       pnpm test:e2e  # playwright (e2e + axe)
       ```

       ## Deployment

       Auto-deploys on push to `main` → `https://wolfwdavid.github.io/michelle_ngo_three/` via GitHub Actions. Production cutover to `michellengo.net` is gated on the `_three` vs `_four` A/B decision (Phase 7).
       ```
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const must=['runed','@sveltejs/enhanced-img','@tailwindcss/typography','@playwright/test','@axe-core/playwright','@testing-library/svelte','@testing-library/jest-dom','@sveltejs/kit','svelte','typescript','tailwindcss','@tailwindcss/vite','vite','vitest','zod','husky','lint-staged','prettier','eslint']; const missing=must.filter(k=>!p.devDependencies[k]); if(missing.length){console.error('MISSING:',missing);process.exit(1)}; if(p.packageManager!=='pnpm@11.0.9'){console.error('packageManager wrong:',p.packageManager);process.exit(1)}; if(!p.engines||p.engines.node!=='>=22'){console.error('engines.node wrong');process.exit(1)}; console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists at the project root and is valid JSON
    - `package.json` contains the string `"packageManager": "pnpm@11.0.9"` (exact)
    - `package.json` contains `"engines"` with `"node": ">=22"`
    - `package.json` `devDependencies` contains ALL of: `runed`, `@sveltejs/enhanced-img`, `@tailwindcss/typography`, `@playwright/test`, `@axe-core/playwright`, `@testing-library/svelte`, `@testing-library/jest-dom` at the exact versions listed in the interfaces block
    - `package.json` `devDependencies` contains ALL 23 sibling pins from the interfaces block at the EXACT version strings (e.g., `"svelte": "5.55.5"`, `"typescript": "5.9.3"`, `"tailwindcss": "4.3.0"`)
    - `package.json` `scripts` contains `dev`, `build`, `check`, `test`, `test:e2e`, `lint`, `format`, `prepare`
    - `.prettierrc` is byte-identical to sibling's `.prettierrc` (verify with `diff` — they MUST match exactly)
    - `.prettierignore` does NOT contain the string `src/lib/data/videos.json` (Phase 2 concern, must be absent in Phase 1)
    - `eslint.config.js` contains the string `tseslint.config(` and the string `eslint-plugin-svelte`
    - `.gitignore` contains both `node_modules` and `/build` and `.svelte-kit`
    - `.npmrc` contains `engine-strict=true`
    - `.nvmrc` contains `22`
    - `README.md` exists and mentions both `michelle_ngo_four` and `wolfwdavid.github.io/michelle_ngo_three/`
  </acceptance_criteria>
  <done>Root tooling exists, package.json validates, prettier/eslint configs match sibling. No deps installed yet (next task).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mirror SvelteKit configs (svelte.config.js, tsconfig.json, vite.config.ts) and install deps</name>
  <files>svelte.config.js, tsconfig.json, vite.config.ts, .husky/pre-commit, pnpm-lock.yaml</files>
  <read_first>
    - C:\Users\Mkaru\Documents\Hello_World\hugginface_profile\Websites\michelle_ngo_three\package.json (created in Task 1)
    - C:\Users\Mkaru\Documents\Hello_World\hugginface_profile\Websites\michelle_ngo_four\svelte.config.js
    - C:\Users\Mkaru\Documents\Hello_World\hugginface_profile\Websites\michelle_ngo_four\tsconfig.json
    - C:\Users\Mkaru\Documents\Hello_World\hugginface_profile\Websites\michelle_ngo_four\vite.config.ts
    - C:\Users\Mkaru\Documents\Hello_World\hugginface_profile\Websites\michelle_ngo_four\.husky\pre-commit
    - C:\Users\Mkaru\Documents\Hello_World\hugginface_profile\Websites\michelle_ngo_three\.planning\phases\01-foundation\01-CONTEXT.md (Established Patterns §Tailwind v4 plugin order — `tailwindcss()` BEFORE `sveltekit()`)
  </read_first>
  <action>
    Create SvelteKit configs (mirror sibling) and run `pnpm install` to materialize `pnpm-lock.yaml` and `node_modules/`. Run `husky` to install the pre-commit hook.

    1. Write `svelte.config.js` byte-identical to the interface block in `<context>` (21 lines, exports `config` with `adapter-static` + `paths.base: process.env.BASE_PATH ?? ''`).

    2. Write `tsconfig.json` byte-identical to the interface block in `<context>` (8 lines, `strict: true` + `noUncheckedIndexedAccess: true` + `noImplicitOverride: true`).

    3. Write `vite.config.ts` (Phase 1 minimal — the validateVideosPlugin is Phase 2 work, DO NOT include it):
       ```ts
       /// <reference types="vitest/config" />
       import { sveltekit } from '@sveltejs/kit/vite';
       import { defineConfig } from 'vite';
       import tailwindcss from '@tailwindcss/vite';

       export default defineConfig({
         // Plugin order matters: tailwindcss BEFORE sveltekit (CONTEXT Established
         // Patterns §Tailwind v4 plugin order). Phase 2 will insert the
         // validateVideosPlugin between tailwindcss() and sveltekit() — leave
         // the array shape easy to extend.
         plugins: [tailwindcss(), sveltekit()],
         test: {
           // Phase 1 minimal vitest config. Plan 01-03 wires the unit smoke test
           // into this surface; Phase 2 will split into data/ui projects.
           include: ['src/**/*.{test,spec}.{js,ts}'],
           environment: 'jsdom',
           globals: false,
         },
       });
       ```

    4. Create `.husky/pre-commit` with the single line `pnpm lint-staged` (matches sibling).

    5. Run `pnpm install`. This populates `node_modules/` and produces `pnpm-lock.yaml`. Use `--ignore-scripts=false` (default) so `husky` runs and installs the git hook.

    6. After install, confirm `node_modules/.bin/svelte-kit` exists and `node_modules/runed/package.json` exists.
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile=false && pnpm exec svelte-kit sync && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - `svelte.config.js` contains the literal string `adapter-static` and `base: process.env.BASE_PATH ?? ''`
    - `tsconfig.json` contains all three: `"strict": true`, `"noUncheckedIndexedAccess": true`, `"noImplicitOverride": true`
    - `tsconfig.json` extends `./.svelte-kit/tsconfig.json`
    - `vite.config.ts` has `plugins: [tailwindcss(), sveltekit()]` with `tailwindcss()` appearing BEFORE `sveltekit()` (verifiable by ordered grep)
    - `vite.config.ts` does NOT yet import from `./src/lib/data/schema` (Phase 2 work — must be absent here)
    - `.husky/pre-commit` file exists and its only non-blank line is `pnpm lint-staged`
    - `pnpm-lock.yaml` exists at the project root (created by `pnpm install`)
    - `node_modules/runed/package.json` exists (proves the new cinematic-layer dep is installed)
    - `node_modules/@sveltejs/enhanced-img/package.json` exists
    - `node_modules/@playwright/test/package.json` exists
    - `node_modules/@axe-core/playwright/package.json` exists
    - `node_modules/@testing-library/svelte/package.json` exists
    - `node_modules/@testing-library/jest-dom/package.json` exists
    - `node_modules/@tailwindcss/typography/package.json` exists
    - `pnpm exec svelte-kit sync` exits 0 (generates `.svelte-kit/tsconfig.json` which `tsconfig.json` extends)
    - `pnpm check` exits 0 with zero errors and zero warnings
  </acceptance_criteria>
  <done>Configs mirrored, lockfile generated, all deps installed, `svelte-kit sync` and `svelte-check` clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create minimal SvelteKit route surface (app.html, +layout, +page, app.css) and verify clean static build</name>
  <files>src/app.html, src/app.css, src/routes/+layout.svelte, src/routes/+layout.ts, src/routes/+page.svelte, src/lib/.gitkeep, static/robots.txt</files>
  <read_first>
    - C:\Users\Mkaru\Documents\Hello_World\hugginface_profile\Websites\michelle_ngo_three\svelte.config.js (created in Task 2)
    - C:\Users\Mkaru\Documents\Hello_World\hugginface_profile\Websites\michelle_ngo_three\vite.config.ts (created in Task 2)
    - C:\Users\Mkaru\Documents\Hello_World\hugginface_profile\Websites\michelle_ngo_four\src\app.html
    - C:\Users\Mkaru\Documents\Hello_World\hugginface_profile\Websites\michelle_ngo_four\src\routes\+layout.svelte (note: sibling imports TopNav/Footer — those are PHASE 4/6; this plan uses a stub layout)
    - C:\Users\Mkaru\Documents\Hello_World\hugginface_profile\Websites\michelle_ngo_four\static\robots.txt
    - C:\Users\Mkaru\Documents\Hello_World\hugginface_profile\Websites\michelle_ngo_three\.planning\phases\01-foundation\01-CONTEXT.md (D-01 splash placeholder shape; D-04 tagline exact string)
  </read_first>
  <action>
    Create the minimum route surface so `pnpm build` actually produces a `build/` directory. The splash content (D-01..D-04 wordmark, fonts, dark canvas) lands in Plan 01-02 — this task only needs ANY valid +page.svelte that uses Tailwind to prove the build path works end-to-end.

    1. Write `src/app.html` byte-identical to the sibling block in `<context>` interfaces (13 lines).

    2. Write `src/app.css` with ONLY the Tailwind v4 import line (the `@theme` block lands in Plan 01-02):
       ```css
       @import "tailwindcss";

       /*
        * Plan 01-02 will populate this file with:
        *   - @theme { --font-display, --font-mono, --font-sans, --ring-focus, --ring-focus-offset, neutrals, 8 OKLCH category accents } (D-02, D-09..D-13)
        *   - Global :focus-visible rule (D-05..D-08)
        *   - @font-face declarations for self-hosted woff2 fonts (D-13)
        * Phase 1 plan 01 ships ONLY the import so `pnpm build` resolves Tailwind utilities.
        */
       ```

    3. Write `src/routes/+layout.svelte` (minimal layout — TopNav/Footer are Phase 4/6):
       ```svelte
       <!--
         Phase 1 plan 01 minimal layout. Imports app.css so Tailwind utilities resolve
         on every route. Noindex meta inherits _four D-11 staging posture (CONTEXT
         Out of Scope: staging-noindex stays through Phase 6 per Pitfall 9).

         Phase 4 (NAV-01) will add <TopNav />; Phase 6 (CONT-03) will add <Footer />.
       -->
       <script lang="ts">
         import '../app.css';
         let { children } = $props();
       </script>

       <svelte:head>
         <meta name="robots" content="noindex, nofollow" />
         <title>Michelle Ngo</title>
       </svelte:head>

       {@render children()}
       ```

    4. Write `src/routes/+layout.ts` to enable prerender for the static adapter:
       ```ts
       // adapter-static requires every route to be prerenderable. Set the default
       // at the layout level so every child route inherits without per-page opt-in.
       export const prerender = true;
       ```

    5. Write `src/routes/+page.svelte` (Phase 1 plan 01 throwaway content — Plan 01-02 replaces with the D-01 wordmark splash):
       ```svelte
       <!--
         Plan 01-02 will replace this with the D-01 wordmark splash
         (Source Serif 4 "MICHELLE NGO" + "Filmmaker. Site coming soon." tagline).
         This stub exists ONLY so `pnpm build` has a valid + prerenderable index
         route to emit.
       -->
       <main class="flex min-h-screen items-center justify-center bg-black text-white">
         <p class="text-sm uppercase tracking-widest">michelle ngo — site coming soon</p>
       </main>
       ```

    6. Create `src/lib/.gitkeep` (empty file) so the directory is tracked. Plan 01-02 will add `src/lib/storage.ts` here.

    7. Write `static/robots.txt` byte-identical to sibling: two lines, `User-agent: *` + `Disallow: /`.

    8. Run `pnpm build` to verify the entire scaffold works end-to-end.
  </action>
  <verify>
    <automated>pnpm build && test -f build/index.html && test -d build/_app && grep -q "michelle ngo" build/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `src/app.html` contains the literal string `%sveltekit.body%` and `meta charset="utf-8"`
    - `src/app.css` contains the literal line `@import "tailwindcss"` (the v4 single-import contract)
    - `src/app.css` does NOT yet contain `@theme` (Plan 01-02's responsibility — out of scope here)
    - `src/routes/+layout.svelte` imports `../app.css` (verifiable by grep)
    - `src/routes/+layout.svelte` contains `<meta name="robots" content="noindex, nofollow" />`
    - `src/routes/+layout.ts` contains `export const prerender = true`
    - `src/routes/+page.svelte` exists and uses at least one Tailwind utility class (e.g., `min-h-screen`, `bg-black`)
    - `src/lib/.gitkeep` exists (empty file or single newline)
    - `static/robots.txt` contains exactly `User-agent: *\nDisallow: /\n` (noindex staging posture)
    - `pnpm build` exits 0 with zero errors AND zero warnings (FOUND-01 — "zero errors, zero warnings on a fresh clone")
    - `build/index.html` is produced by the static adapter and contains the splash placeholder string `michelle ngo`
    - `build/_app/` directory exists and contains hashed JS/CSS assets (Tailwind utilities compiled successfully)
  </acceptance_criteria>
  <done>Scaffold builds cleanly to `build/`. `pnpm build` exits 0 with no warnings. Tailwind utilities resolve. Foundation is complete and ready for Plan 01-02 (`_three`-specific tokens + storage helper + splash content) and Plan 01-03 (deploy workflow + smoke tests).</done>
</task>

</tasks>

<verification>
After all three tasks complete, run on a hypothetically fresh clone:
1. `pnpm install --frozen-lockfile` — must exit 0 (proves the lockfile is complete and reproducible)
2. `pnpm check` — must exit 0 with no errors (proves TS strict + noUncheckedIndexedAccess + noImplicitOverride + Svelte 5 runes type-check cleanly)
3. `pnpm build` — must exit 0 with no warnings (proves Tailwind v4 + adapter-static + plugin order work; FOUND-01 satisfied)
4. `pnpm lint` — must exit 0 (proves ESLint flat config is wired correctly)

Grep checks (Success Criteria #1 verification):
- `grep -r "noUncheckedIndexedAccess" tsconfig.json` returns a match
- `grep "tailwindcss(), sveltekit()" vite.config.ts` returns a match (plugin order)
- `grep "runed" package.json` returns a match (proves cinematic-layer additions present)
</verification>

<success_criteria>
- `package.json` is byte-identical to sibling's locked deps PLUS the 7 cinematic-layer additions (runed, @sveltejs/enhanced-img, @tailwindcss/typography, @playwright/test, @axe-core/playwright, @testing-library/svelte, @testing-library/jest-dom)
- `pnpm install && pnpm build` produces a clean `build/` directory with `index.html` + `_app/` assets
- `pnpm check` exits 0 (TypeScript strict + noUncheckedIndexedAccess + noImplicitOverride active)
- Vite plugin order is `tailwindcss() → sveltekit()` (CONTEXT Established Patterns §Tailwind v4 plugin order)
- `pnpm-lock.yaml` is committed (required for the deploy workflow's `pnpm install --frozen-lockfile` in Plan 01-03)
- All sibling tooling files (`.prettierrc`, `eslint.config.js`, `svelte.config.js`, `tsconfig.json`, `.husky/pre-commit`, `.prettierignore`) match `_four`'s shape (per CONTEXT Claude's-Discretion "match _four exactly")
- Husky pre-commit hook is active and runs `pnpm lint-staged`
- FOUND-01 satisfied: clean static build with TS strict, Svelte 5 runes, Tailwind v4
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` per `~/.claude/get-shit-done/templates/summary.md`. Capture:
- Exact dep versions installed (for cross-reference if a future plan needs to bump)
- Any deviation from the sibling (there should be none in shared files; ONLY the 7 cinematic-layer additions + the `michelle-ngo-three` name + the `test:e2e` script + the dropped Phase 2 scripts)
- Confirmation that `pnpm build` produces zero warnings on a fresh clone
</output>
