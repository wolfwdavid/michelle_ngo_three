---
phase: 07-polish-cutover
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - static/favicon.ico
  - static/favicon-16.png
  - static/favicon-32.png
  - static/favicon-192.png
  - static/favicon-512.png
  - static/apple-touch-icon.png
  - static/og-image.jpg
  - static/.nojekyll
autonomous: false
requirements: [POL-01, POL-05]
nyquist_compliant: true

must_haves:
  truths:
    - "Every favicon link in the layout head resolves to a real file in static/ (no 404s in the browser tab / iOS home screen)"
    - "The OG image is a 1200x630 cinematic-dark composition that is _three's OWN art (not a reuse of _four's editorial art)"
    - "The OG image dimensions are byte-identical (1200x630) to _four's so social crops render identically across both A/B siblings (Trap B)"
    - "GitHub Pages does not strip SvelteKit's _app/ immutable assets (Jekyll disabled via .nojekyll)"
  artifacts:
    - path: "static/og-image.jpg"
      provides: "1200x630 cinematic-dark OG/Twitter card image (Trap B dimensional-parity asset)"
    - path: "static/favicon.ico"
      provides: "Multi-size favicon (16+32) for legacy browser tabs"
    - path: "static/favicon-512.png"
      provides: "512px PNG favicon (Android home-screen / PWA)"
    - path: "static/apple-touch-icon.png"
      provides: "180x180 iOS home-screen icon"
    - path: "static/.nojekyll"
      provides: "Disables GH Pages Jekyll processing (protects _app/ assets — Pitfall 5)"
  key_links:
    - from: "src/routes/+layout.svelte"
      to: "static/favicon-192.png"
      via: "favicon link href resolves to static asset"
      pattern: "favicon-192"
    - from: "static/og-image.jpg"
      to: "../michelle_ngo_four/static/og-image.jpg"
      via: "1200x630 dimensional parity (Trap B)"
      pattern: "1200.*630"
---

<objective>
Author the cinematic-dark binary assets the POL-01 metadata block references: the multi-size favicon set (6 files), the 1200×630 OG image, and the `.nojekyll` GH Pages guard. The favicon/OG art is `_three`'s OWN cinematic-dark composition (D-02/D-13) — NOT a reuse of `_four`'s editorial art — but mitigated for the A/B by DIMENSIONAL parity (identical 1200×630 + comparable filesize so social crops match, Trap B).

Purpose: Plan 07-01's layout head block and coverage script reference these binaries. They're separated into their own plan because (a) the file set is `static/` binaries only (zero overlap with 07-01's `src/` work → parallel-safe) and (b) real cinematic asset authoring is a human-judgment / design decision that needs a checkpoint per D-13 + RESEARCH Open Question 1.
Output: 6 favicon files + og-image.jpg + .nojekyll in `static/`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-polish-cutover/07-CONTEXT.md
@.planning/phases/07-polish-cutover/07-RESEARCH.md

<interfaces>
<!-- The layout head (Plan 07-01) references these exact filenames — they MUST exist with these names. -->
Referenced by src/routes/+layout.svelte favicon/OG block:
  static/favicon.ico, favicon-16.png, favicon-32.png, favicon-192.png, favicon-512.png,
  apple-touch-icon.png (180x180), og-image.jpg (1200x630)

<!-- The coverage script (Plan 07-01) asserts these exact filenames exist in build/. -->
scripts/test-prerender-coverage.mjs requiredStaticAssets = [
  'favicon.ico','favicon-16.png','favicon-32.png','favicon-192.png','favicon-512.png',
  'apple-touch-icon.png','og-image.jpg' ]

<!-- Trap B parity target (Plan 07-04 diffs against this). -->
../michelle_ngo_four/static/og-image.jpg  (1200x630, 15386 bytes — _four's asset; _three's must match DIMENSIONS, comparable filesize)

<!-- Available source material for cinematic-dark crops (no design-tool dep needed). -->
static/posters/*.jpg  (existing hero/reel poster JPEGs — crop source per Claude's Discretion in CONTEXT)
static/fonts/*.woff2   (7 self-hosted fonts — usable for wordmark at author time, not a runtime dep)
sharp 0.34.x (already on disk via @sveltejs/enhanced-img) — usable for a one-shot scripts/build-assets.mjs
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide OG/favicon asset authoring approach (cinematic-real vs sharp-script vs placeholder)</name>
  <files>(no files — decision gate only; Task 2 produces the binaries per the choice)</files>
  <action>Pause and present the three asset-authoring approaches below; capture the user's selection before Task 2 produces any binaries. This is a decision gate only — no files change in this task. Whatever is chosen, the constraint is fixed: og-image.jpg MUST be 1200×630 (Trap B) and the 6 favicon filenames MUST exist (or the coverage gate fails + browser tabs 404).</action>
  <decision>How to produce the cinematic-dark favicon set + 1200×630 OG image</decision>
  <context>
    D-02/D-13 want _three's OWN cinematic-dark art (the A/B tests design, so the visual SHOULD differ from _four). Claude's Discretion (CONTEXT) allows shipping a hero-poster crop as-is if compositing complicates. The kickoff decision "both A/B siblings get full polish" leans toward real assets; `_four` itself shipped 67-byte placeholders and deferred. The metadata WIRING (Plan 07-01 layout block + coverage script) proceeds regardless — only the binary QUALITY is gated here. Whatever is chosen, the OG image MUST be 1200×630 (Trap B dimensional parity) and the favicon set MUST exist with the exact 6 filenames (or the coverage gate + browser tab 404s).
  </context>
  <options>
    <option id="cinematic-real">
      <name>Author real cinematic-dark assets</name>
      <pros>Honors D-02/D-13 + "full polish" kickoff; _three ships its own distinct social card; strongest A/B-as-design statement</pros>
      <cons>Requires image-editor work + realfavicongenerator.net; most wall-clock</cons>
    </option>
    <option id="sharp-script">
      <name>sharp one-shot crop script (scripts/build-assets.mjs)</name>
      <pros>Reproducible; derives 1200×630 OG + favicon master from an existing static/posters/*.jpg crop; no new runtime dep (sharp already on disk); honors "ship the crop as-is" discretion</pros>
      <cons>Crop-as-is is less polished than hand-composited; still _three's own frame (cinematic poster), so satisfies "own art"</cons>
    </option>
    <option id="placeholder-backlog">
      <name>Placeholder set + post-launch backlog (the _four branch)</name>
      <pros>Fastest; unblocks the coverage gate + cutover infra immediately; matches what _four actually shipped</pros>
      <cons>Violates the _three-prescriptive D-02/D-13 "own cinematic art" intent; defers the visible social-card quality; must be tracked as a pre-cutover backlog item in 07-QA-MATRIX</cons>
    </option>
  </options>
  <resume-signal>Select: cinematic-real, sharp-script, or placeholder-backlog</resume-signal>
  <acceptance_criteria>
    - User has selected one of the three approaches; the choice is recorded in the plan SUMMARY decisions section with rationale
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Produce the favicon set + 1200x630 og-image.jpg + .nojekyll per the chosen approach</name>
  <files>static/favicon.ico, static/favicon-16.png, static/favicon-32.png, static/favicon-192.png, static/favicon-512.png, static/apple-touch-icon.png, static/og-image.jpg, static/.nojekyll (optionally scripts/build-assets.mjs if sharp-script approach)</files>
  <read_first>
    - ../michelle_ngo_four/static/ (the asset set + sizes to match: favicon.ico, favicon-{16,32,192,512}.png, apple-touch-icon.png, og-image.jpg 1200x630 15386 bytes, .nojekyll)
    - src/routes/+layout.svelte (the exact filenames the head block references — Plan 07-01 wired these)
    - scripts/test-prerender-coverage.mjs (the requiredStaticAssets array — these exact 7 filenames must exist)
    - static/posters/ (crop source if sharp-script or cinematic-real derives from a poster)
  </read_first>
  <action>
    Execute the approach chosen in Task 1. In ALL cases produce these exact files in `static/`:
    - `favicon.ico` (multi-res 16+32)
    - `favicon-16.png`, `favicon-32.png`, `favicon-192.png`, `favicon-512.png`
    - `apple-touch-icon.png` (180×180)
    - `og-image.jpg` (MUST be exactly 1200×630 — Trap B; comparable filesize to _four's ~15KB, within a 3× ratio band so the Plan 07-04 Trap B gate passes)
    - `static/.nojekyll` (empty file — Pitfall 5; GH Pages Jekyll guard protecting `_app/`)

    Approach detail:
    - cinematic-real: author a 512×512 cinematic-dark favicon master + a 1200×630 cinematic-dark OG composition (dark canvas + Michelle Ngo wordmark using the self-hosted font, optional hero-poster motif), then export all sizes (realfavicongenerator.net or any editor). Keep `static/favicon.png` as the existing fallback.
    - sharp-script: write a dev-only `scripts/build-assets.mjs` using sharp (already on disk) that crops a `static/posters/*.jpg` to 1200×630 → og-image.jpg and resizes a master to the 6 favicon sizes. Run it; commit the OUTPUT binaries (the script itself is optional to keep).
    - placeholder-backlog: copy `_four`'s asset set as placeholders (or generate solid cinematic-dark squares), ensure og-image.jpg is exactly 1200×630, and record a "real cinematic OG/favicon authoring" item in Plan 07-05's 07-QA-MATRIX pre-cutover backlog (NOT a launch blocker per the _four branch).

    Verify the OG dimensions immediately with an image probe (sharp metadata, ImageMagick `identify`, or a JPEG SOF0 header parse) — width MUST read 1200, height MUST read 630.
  </action>
  <verify>
    <automated>pnpm build && node scripts/test-prerender-coverage.mjs</automated>
  </verify>
  <acceptance_criteria>
    - All 7 files exist in static/: `test -f static/favicon.ico && test -f static/favicon-16.png && test -f static/favicon-32.png && test -f static/favicon-192.png && test -f static/favicon-512.png && test -f static/apple-touch-icon.png && test -f static/og-image.jpg` (all pass)
    - `static/.nojekyll` exists
    - og-image.jpg dimensions are EXACTLY 1200×630 (probe: `node -e "require('sharp')('static/og-image.jpg').metadata().then(m=>{if(m.width!==1200||m.height!==630){process.exit(1)}})"` exits 0)
    - After `pnpm build`: all 7 assets present in `build/` AND `build/.nojekyll` exists
    - `node scripts/test-prerender-coverage.mjs` exits 0 (the requiredStaticAssets check now passes — was the only failing assertion in Wave 1)
    - If placeholder-backlog chosen: a backlog line is recorded for Plan 07-05's 07-QA-MATRIX
  </acceptance_criteria>
  <done>6 favicons + 1200×630 og-image + .nojekyll in static/; coverage gate fully green; OG dimensions verified 1200×630 for Trap B.</done>
</task>

</tasks>

<verification>
- `node scripts/test-prerender-coverage.mjs` exits 0 (all 7 required static assets present in build/)
- OG image probes at exactly 1200×630 (Trap B parity precondition)
- `build/.nojekyll` exists (GH Pages _app/ protection)
- No browser-tab/iOS favicon 404s (every layout favicon link resolves)
</verification>

<success_criteria>
- Favicon set (6 files) + og-image.jpg (1200×630) + .nojekyll exist in static/ and copy into build/
- OG image is _three's own cinematic-dark art at 1200×630 with comparable filesize to _four (Trap B mitigated)
- The Plan 07-01 coverage gate's static-asset assertion now passes fully
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-cutover/07-02-SUMMARY.md`
</output>
