---
phase: 07-polish-cutover
plan: 05
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - static/CNAME
  - .github/workflows/deploy-production.yml
  - .planning/phases/07-polish-cutover/07-QA-MATRIX.md
autonomous: false
requirements: [FOUND-03, POL-04]
nyquist_compliant: true

must_haves:
  truths:
    - "static/CNAME contains michellengo.net and copies into every build artifact"
    - "A manual-dispatch production workflow builds with BASE_PATH='' for the apex domain (staging deploy.yml unchanged)"
    - "The noindex→index atomic flip (D-12) is documented as a single prepared commit but NOT landed (winner-only, last commit before DNS)"
    - "A single 07-QA-MATRIX.md consolidates Phase 3 reel matrix + Phase 5 surface UAT + 21-cell responsive sweep + CONT-02 swap as the go/no-go cutover gate"
    - "A 9-step Launch Runbook is reviewable end-to-end, gated, and reversible (DNS-revert rollback, TTL=300s)"
  artifacts:
    - path: "static/CNAME"
      provides: "GitHub Pages apex-domain assertion baked into every build artifact"
      contains: "michellengo.net"
    - path: ".github/workflows/deploy-production.yml"
      provides: "Manual-dispatch production deploy (BASE_PATH='') with Verify-CNAME guard"
      contains: "workflow_dispatch"
    - path: ".planning/phases/07-polish-cutover/07-QA-MATRIX.md"
      provides: "Consolidated go/no-go cutover gate (reel matrix + surface UAT + 21-cell + CONT-02 + Lighthouse blocking-flip)"
      contains: "BrowserStack"
  key_links:
    - from: ".github/workflows/deploy-production.yml"
      to: "static/CNAME"
      via: "Verify CNAME in build artifact guard step"
      pattern: "build/CNAME"
    - from: "07-QA-MATRIX.md"
      to: "03-HUMAN-UAT.md + 05-HUMAN-UAT.md"
      via: "consolidation of deferred QA backlog (D-05)"
      pattern: "thermal\\|surface UAT"
---

<objective>
Stage all cutover infrastructure so `_three` is A/B-winner-ready and reversible — WITHOUT firing the live cutover (D-09 hard-gates the DNS flip on the user's explicit "_three wins" declaration; D-06 hard-gates it on a GREEN BrowserStack matrix). Deliver: `static/CNAME`, `deploy-production.yml` (BASE_PATH='' manual-dispatch), the prepared-but-unlanded D-12 atomic noindex→index flip, the consolidated `07-QA-MATRIX.md` go/no-go gate, and the 9-step Launch Runbook (in the SUMMARY).

Purpose: FOUND-03 = "infrastructure ready, reviewable, reversible, A/B-winner-gated." This mirrors `_four`'s 07-05 stop-at-infrastructure pattern: the runbook IS the deliverable for the human-action cutover steps. POL-04's manual half (BrowserStack 7-OS + thermal + 21-cell + 7 surface UAT) folds into the single 07-QA-MATRIX.md per D-05, superseding the partial 03-/05-HUMAN-UAT trackers. The CONT-02 IMDb/LinkedIn personalized-URL swap (pre-cutover blocker) is tracked in the matrix checklist.
Output: CNAME + deploy-production.yml + 07-QA-MATRIX.md + Launch Runbook (in SUMMARY).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-polish-cutover/07-CONTEXT.md
@.planning/phases/07-polish-cutover/07-RESEARCH.md
@.planning/phases/03-reel-system-core-load-bearing-risk/03-HUMAN-UAT.md
@.planning/phases/05-hero-watch/05-HUMAN-UAT.md

<interfaces>
<!-- _four reference implementations (PORT these — adjust repo name three vs four). -->
- ../michelle_ngo_four/.github/workflows/deploy-production.yml (63-line manual-dispatch production deploy, BASE_PATH='', Verify-CNAME guard, shared concurrency.group: pages)
- ../michelle_ngo_four/static/CNAME (1 line: michellengo.net)
- ../michelle_ngo_four/.planning/phases/07-polish-production-cutover/07-05-production-cutover-SUMMARY.md §Launch Runbook (the 9-step gated reversible runbook to adapt — repo URL wolfwdavid/michelle_ngo_three, BASE_PATH=/michelle_ngo_three staging)
- ../michelle_ngo_four/.planning/phases/07-polish-production-cutover/07-QA-MATRIX.md (the 21-cell matrix doc shape to adopt for D-08)

<!-- _three current state (VERIFIED) the cutover touches. -->
- svelte.config.js: paths.base = process.env.BASE_PATH ?? '' (the per-env hook — already present)
- .github/workflows/deploy.yml: staging, BASE_PATH=/${{ github.event.repository.name }} (=/michelle_ngo_three) — STAYS UNCHANGED
- src/routes/+layout.svelte: <meta name="robots" content="noindex, nofollow" /> at line 51 — D-12 flip removes this (winner-only, NOT in this plan)
- static/robots.txt: "User-agent: *\nDisallow: /" — D-12 flip → Allow + Sitemap directive (winner-only, NOT in this plan)
- src/lib/components/ContactBlock.svelte: IMDB_URL/LINKEDIN_URL are channel homepages (CONT-02 swap = QA-matrix checklist item)

<!-- Deferred QA backlog to consolidate (D-05). -->
- 03-HUMAN-UAT.md: BrowserStack 7-OS × 4-pillar reel matrix + iPhone 5-min thermal (delta ≤ 8%) — both blocked pending pre-cutover
- 05-HUMAN-UAT.md: 7 surface UAT items (hero attach/play/unmount, watch chrome-fade, HERO-03 sound-on, WATCH-05 back-nav + cross-route rail restore, axe staging spot-check)

<!-- GH Pages apex anycast IPs (runbook DNS step). -->
185.199.108.153 / 185.199.109.153 / 185.199.110.153 / 185.199.111.153 ; www CNAME → wolfwdavid.github.io
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create static/CNAME + deploy-production.yml (BASE_PATH='', manual-dispatch, Verify-CNAME guard)</name>
  <files>static/CNAME, .github/workflows/deploy-production.yml</files>
  <read_first>
    - ../michelle_ngo_four/.github/workflows/deploy-production.yml (the verbatim source — adjust nothing except it's already repo-agnostic; confirm BASE_PATH='', workflow_dispatch, Verify-CNAME guard, concurrency.group: pages)
    - ../michelle_ngo_four/static/CNAME (1 line michellengo.net)
    - .github/workflows/deploy.yml (the staging workflow — confirm it STAYS unchanged; shares concurrency.group: pages with the new production workflow so they cannot race)
    - svelte.config.js (confirm paths.base = process.env.BASE_PATH ?? '' — the single per-env hook; no source branches needed)
  </read_first>
  <action>
    (A) Create `static/CNAME` with exactly one line + trailing newline: `michellengo.net`. adapter-static copies `static/` → `build/`, so `build/CNAME` ships in every artifact (harmless on staging — GH Pages ignores it there).

    (B) Create `.github/workflows/deploy-production.yml` by porting `_four`'s verbatim. Concrete contract:
    - `name: Deploy to GitHub Pages (production / apex)`
    - `on: workflow_dispatch:` ONLY (production fires only on manual dispatch — D-11 verify-then-flip; never auto-fires on push)
    - `permissions: contents: read, pages: write, id-token: write`
    - `concurrency: { group: pages, cancel-in-progress: false }` (SHARED with staging deploy.yml so they never race)
    - build job: checkout → setup-node@v4 node 22 → pnpm/action-setup@v4 version 11.0.9 standalone → `pnpm install --frozen-lockfile` → Build step with `env: { BASE_PATH: '' }` running `pnpm build` → `Verify CNAME in build artifact` step (`test -f build/CNAME || (echo "build/CNAME missing — D-11 assertion not in artifact" && exit 1)` + echo the content) → `actions/upload-pages-artifact@v3 path: build/`
    - deploy job: `needs: build`, environment github-pages, `actions/deploy-pages@v4`

    Do NOT modify `.github/workflows/deploy.yml` (staging stays BASE_PATH=/michelle_ngo_three on push-to-main). Both share concurrency group `pages`.
  </action>
  <verify>
    <automated>test -f static/CNAME && grep -qx michellengo.net static/CNAME && grep -c "BASE_PATH: ''" .github/workflows/deploy-production.yml</automated>
  </verify>
  <acceptance_criteria>
    - `static/CNAME` exists AND `grep -qx 'michellengo.net' static/CNAME` succeeds (exactly that one line)
    - `.github/workflows/deploy-production.yml` exists AND `grep -c "BASE_PATH: ''" .github/workflows/deploy-production.yml` == 1
    - `grep -c 'workflow_dispatch' .github/workflows/deploy-production.yml` ≥ 1 AND the workflow has NO `push:` trigger (production never auto-fires — only manual dispatch)
    - `grep -c 'Verify CNAME in build artifact\|build/CNAME' .github/workflows/deploy-production.yml` ≥ 1
    - `grep -c 'group: pages' .github/workflows/deploy-production.yml` ≥ 1 (shared concurrency)
    - `.github/workflows/deploy.yml` staging line UNCHANGED: `grep -c 'BASE_PATH: /\${{ github.event.repository.name }}' .github/workflows/deploy.yml` == 1
    - After `pnpm build`: `test -f build/CNAME && grep -qx michellengo.net build/CNAME` (CNAME ships in the artifact)
  </acceptance_criteria>
  <done>static/CNAME + deploy-production.yml created; staging deploy.yml untouched; CNAME ships in build artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Create consolidated 07-QA-MATRIX.md go/no-go cutover gate (D-05/D-06/D-07/D-08 + CONT-02)</name>
  <files>.planning/phases/07-polish-cutover/07-QA-MATRIX.md</files>
  <read_first>
    - ../michelle_ngo_four/.planning/phases/07-polish-production-cutover/07-QA-MATRIX.md (the 21-cell matrix doc shape — F/S/T/I/X/C check categories, breakpoints, real-iOS spot-check, punch-list + fix-log sections)
    - .planning/phases/03-reel-system-core-load-bearing-risk/03-HUMAN-UAT.md (BrowserStack 7-OS × 4-pillar matrix + iPhone thermal — the rows to fold in)
    - .planning/phases/05-hero-watch/05-HUMAN-UAT.md (the 7 surface UAT items — the rows to fold in)
    - .planning/phases/03-reel-system-core-load-bearing-risk/03-VERIFICATION.md (the 28-cell BrowserStack matrix template + escalation Branch A [360p ±1 cap] / Branch B [D-09 reversal to current-only-plays])
    - src/lib/components/ContactBlock.svelte (CONT-02 IMDb/LinkedIn channel-homepage URLs — the swap is a pre-cutover checklist row)
  </read_first>
  <action>
    Create `.planning/phases/07-polish-cutover/07-QA-MATRIX.md` as the SINGLE go/no-go cutover gate (D-05). It supersedes the partial 03-/05-HUMAN-UAT trackers. Structure (adapt `_four`'s shape):

    1. YAML frontmatter: `status: pending`, `phase: 07-polish-cutover`, `plan: 07-05`, `gates_cutover: true`.
    2. **§ BrowserStack 7-OS × 4-pillar reel matrix (D-06)** — the 28-cell matrix (7 OS: iOS Safari 16 / 17.0 / 17.1 / 17.2+ / Chrome Android / Firefox desktop / Safari macOS × 4 pillars: scroll-snap fluidity / iframe lifecycle leak-free / poster-fallback degrade / Page-Visibility pause) with a status column (all `?` pending). Note the gate: ALL green before cutover. Reference escalation Branch A (360p ±1 quality cap) + Branch B (D-09 reversal to current-only-plays) from 03-VERIFICATION.md.
    3. **§ iPhone 5-min thermal test (D-06)** — physical iPhone, 5-min reel scroll, temp delta ≤ 8% pass criterion; status `?`.
    4. **§ 7 Phase 5 surface UAT items (D-07)** — table of the 7 items from 05-HUMAN-UAT.md (hero attach/play, hero unmount-to-poster, watch chrome-fade on real Vimeo, HERO-03 sound-on autoplay, WATCH-05 back-nav reel restore, cross-route rail restore, axe staging spot-check); each `?` pending; note they fold into the SAME BrowserStack/iPhone hardware pass.
    5. **§ 21-cell responsive sweep (D-08)** — the 7-routes × 3-breakpoints (mobile ≤640 / tablet ~768 / desktop ≥1280) matrix with the F/S/T/I/X/C check categories (copy `_four`'s legend), a Punch List section, and a Fix Log section. Methodology: Chrome DevTools mobile emulation primary + the real-iPhone pass as the iOS spot-check (real-Android NOT required — DevTools emulation is the Android contract).
    6. **§ Pre-cutover checklist (go/no-go)** — the blocking rows that must all be GREEN before the user fires cutover:
       - [ ] BrowserStack 28-cell matrix all green (D-06)
       - [ ] iPhone thermal delta ≤ 8% (D-06)
       - [ ] 7 surface UAT items pass (D-07)
       - [ ] 21-cell responsive sweep: all cells pass OR punch-list items all resolved (D-08)
       - [ ] CONT-02: IMDb/LinkedIn personalized URLs swapped in ContactBlock.svelte (currently channel homepages) — OR explicitly accepted as channel-homepage at launch (the _four branch)
       - [ ] Lighthouse `/` LCP gate flipped warn→error in lighthouserc.json + deploy.yml (D-17 blocking pre-cutover posture)
       - [ ] All 5 trap CI gates green (A/B/C/D/E)
       - [ ] A/B winner declared by the user (D-09) — the hard gate that unlocks the runbook
    Mark this document `status: pending` — it is filled in DURING the human cutover-prep window, not now. This plan DELIVERS the gate document; the user executes the QA runs against it.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-polish-cutover/07-QA-MATRIX.md && grep -c "BrowserStack" .planning/phases/07-polish-cutover/07-QA-MATRIX.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07-polish-cutover/07-QA-MATRIX.md` exists with `status: pending` frontmatter AND `gates_cutover: true`
    - Contains all 5 sections: `grep -c 'BrowserStack' …07-QA-MATRIX.md` ≥ 1, `grep -c 'thermal' …` ≥ 1, `grep -c 'surface UAT\|surface UAT items' …` ≥ 1, `grep -c '21-cell\|responsive sweep' …` ≥ 1, `grep -c 'Pre-cutover checklist\|go/no-go' …` ≥ 1
    - The pre-cutover checklist includes the CONT-02 row (`grep -c 'IMDb\|LinkedIn\|CONT-02' …` ≥ 1), the Lighthouse blocking-flip row (`grep -c 'warn→error\|blocking' …` ≥ 1), the 5-trap row, and the A/B-winner row (`grep -c 'A/B winner\|D-09' …` ≥ 1)
    - References the escalation Branch A / Branch B from 03-VERIFICATION.md (`grep -c '360p\|Branch A\|Branch B\|current-only' …` ≥ 1)
  </acceptance_criteria>
  <done>07-QA-MATRIX.md consolidates all manual QA (reel matrix + thermal + surface UAT + 21-cell + CONT-02 + blocking-flip + winner gate) as the single go/no-go cutover gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Review cutover readiness — Launch Runbook, staged atomic flip, A/B-winner gate (FOUND-03 stop-at-infrastructure)</name>
  <files>(no files change — review gate; the D-12 atomic flip is deliberately NOT landed; the 9-step runbook is authored in 07-05-SUMMARY.md)</files>
  <action>Author the 9-step Launch Runbook into 07-05-SUMMARY.md (adapted from _four's 07-05, repo wolfwdavid/michelle_ngo_three, staging BASE_PATH=/michelle_ngo_three), then pause for the user to review cutover readiness. Confirm the D-12 atomic flip is documented as ONE prepared commit but NOT landed. No source files change in this task — the live DNS flip is the user's cutover-day action, gated on the A/B-winner declaration (D-09) + GREEN BrowserStack matrix (D-06).</action>
  <what-built>
    Cutover infrastructure: `static/CNAME` (michellengo.net, ships in every build artifact), `deploy-production.yml` (manual-dispatch, BASE_PATH='', Verify-CNAME guard, shared concurrency with staging), and `07-QA-MATRIX.md` (the consolidated go/no-go gate). The 9-step Launch Runbook is authored in the SUMMARY (adapted from `_four`'s 07-05: pre-flight push → trigger prod build → add custom domain → cert wait → curl --resolve apex verify → atomic noindex+robots flip [D-12 one-way door] → re-trigger flipped build → registrar DNS swap → post-cutover verify; + DNS-revert rollback with TTL=300s). The D-12 atomic flip (remove noindex from +layout.svelte + flip robots.txt to Allow + Sitemap) is documented as a SINGLE prepared commit but deliberately NOT landed — it is winner-only, the LAST commit before DNS, fired by the user on cutover day (Pitfall 6: never split it; never land it during the A/B).
  </what-built>
  <how-to-verify>
    1. Read the Launch Runbook in `07-05-SUMMARY.md`. Confirm all 9 steps are gated (do-not-proceed-past-a-failed-step), the curl --resolve verification commands are present (apex serves GH Pages NOT WordPress before DNS), and the rollback path (registrar DNS revert, TTL=300s) is documented.
    2. Confirm the D-12 atomic flip is described as ONE commit staging BOTH `src/routes/+layout.svelte` (delete noindex line) AND `static/robots.txt` (Allow + Sitemap directive), and that it is NOT landed in this phase (verify: `grep -c noindex src/routes/+layout.svelte` still ≥ 1; `grep -c 'Disallow' static/robots.txt` still ≥ 1).
    3. Confirm cutover is hard-gated: it fires ONLY after (a) the user declares `_three` the A/B winner (D-09) AND (b) the 07-QA-MATRIX BrowserStack/thermal gate is GREEN (D-06). Phase 7 delivers readiness; the DNS flip is the user's cutover-day action, outside autonomous scope.
    Approve that the infrastructure + runbook are complete and reviewable, OR direct changes. Note: the BrowserStack runs, the 21-cell sweep, the CONT-02 swap decision, and the A/B-winner declaration are all the USER's to execute against 07-QA-MATRIX.md — they are NOT done here.
  </how-to-verify>
  <resume-signal>Type "approved — cutover-ready, stop at infrastructure" OR direct changes to the runbook/infra</resume-signal>
  <acceptance_criteria>
    - User has reviewed the Launch Runbook + staged-flip + winner-gate and approved the stop-at-infrastructure terminal state (or directed changes)
    - Confirmed NOT landed: `grep -c 'noindex' src/routes/+layout.svelte` ≥ 1 AND `static/robots.txt` still contains `Disallow: /` (the D-12 flip is winner-only, not fired this phase)
    - The 9-step Launch Runbook is present in 07-05-SUMMARY.md with gated steps + curl-verify + DNS-revert rollback
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `static/CNAME` == michellengo.net; ships in `build/CNAME`
- `deploy-production.yml` present (BASE_PATH='', workflow_dispatch only, Verify-CNAME guard, shared concurrency); staging deploy.yml unchanged
- `07-QA-MATRIX.md` consolidates reel matrix + thermal + 7 surface UAT + 21-cell + CONT-02 + blocking-flip + winner gate (status: pending)
- D-12 atomic flip documented as ONE prepared commit, NOT landed (noindex + Disallow still present)
- 9-step Launch Runbook reviewable, gated, reversible (in SUMMARY)
</verification>

<success_criteria>
- FOUND-03 cutover infrastructure ready, reviewable, reversible, A/B-winner-gated (CNAME + deploy-production.yml + runbook)
- POL-04 manual QA consolidated into the single 07-QA-MATRIX.md go/no-go gate (D-05)
- D-12 atomic noindex→index flip prepared but NOT landed (winner-only, last commit before DNS — Pitfall 6)
- Cutover delivers READINESS only — the live DNS flip is hard-gated on the user's A/B-winner declaration (D-09) + GREEN BrowserStack matrix (D-06), never autonomous
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-cutover/07-05-SUMMARY.md` (MUST include the full 9-step Launch Runbook + DNS-revert rollback — the runbook IS the deliverable for the human-action cutover steps)
</output>
