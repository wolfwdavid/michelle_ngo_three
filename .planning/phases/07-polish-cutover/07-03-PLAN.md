---
phase: 07-polish-cutover
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/axe.spec.ts
  - lighthouserc.json
  - .planning/phases/07-polish-cutover/07-LIGHTHOUSE.json
autonomous: false
requirements: [POL-02, POL-03, POL-04]
nyquist_compliant: true

must_haves:
  truths:
    - "axe-core scans all 7 routes (not just /) and reports zero WCAG AA violations"
    - "Every scroll-snap section uses 100svh (not vh, not dvh) — grep-verifiable, zero violations"
    - "The poster→iframe swap contributes ≤ 0 measurable CLS (shared aspect-video container)"
    - "The / LCP on simulated 4G is measured and committed to 07-LIGHTHOUSE.json (warning-only initial posture; escalate-or-accept at checkpoint if > 2.5s)"
  artifacts:
    - path: "tests/e2e/axe.spec.ts"
      provides: "axe WCAG AA scan across all 7 routes"
      contains: "/work"
    - path: "lighthouserc.json"
      provides: "LHCI config asserting / LCP < 2500ms on mobile Slow-4G (warning posture)"
      contains: "largest-contentful-paint"
    - path: ".planning/phases/07-polish-cutover/07-LIGHTHOUSE.json"
      provides: "Real Lighthouse measurement of / LCP + CLS (D-17 measure-first artifact)"
  key_links:
    - from: "tests/e2e/axe.spec.ts"
      to: "all 7 routes"
      via: "AxeBuilder scan per route"
      pattern: "AxeBuilder"
    - from: "lighthouserc.json"
      to: "/ LCP budget"
      via: "largest-contentful-paint maxNumericValue 2500"
      pattern: "2500"
---

<objective>
Harden the existing `/`-only axe smoke gate to all 7 routes (POL-04), grep-verify the POL-03 `100svh` + zero-CLS contract (already structurally satisfied — this is an audit, not a refactor), and ACTUALLY MEASURE the `/` LCP on simulated 4G per D-17 "measure first" — committing real numbers to `07-LIGHTHOUSE.json` with a checkpoint to escalate-or-accept if it misses 2.5s.

Purpose: POL-04 axe is already wired (`tests/e2e/axe.spec.ts`, `/`-only) — extending to 7 routes may surface latent violations (Pitfall 7: iframe titles, accent-token contrast, sticky-pill aria-current). POL-03 is already compliant structurally (ReelSection/PosterImage/PreviewLoop share `aspect-video w-full`; `h-svh`/`calc(100svh-…)` pervasive) — POL-03 here is a grep gate + Lighthouse CLS confirm. POL-02 diverges from `_four`'s deferral: D-17 is prescriptive ("measure first"), and the LCP element is the hero POSTER (not a static WebP `<picture>`) — so `_four`'s AVIF escalation does NOT map (Pitfall 3).
Output: 7-route axe spec, `lighthouserc.json`, real `07-LIGHTHOUSE.json`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-polish-cutover/07-CONTEXT.md
@.planning/phases/07-polish-cutover/07-RESEARCH.md
@.planning/phases/07-polish-cutover/07-VALIDATION.md

<interfaces>
<!-- Current axe spec (VERIFIED) — scans / only; extend the route set. -->
tests/e2e/axe.spec.ts today:
```typescript
test('home / has zero axe accessibility violations', async ({ page }) => {
  await page.goto('/');
  const results = await new AxeBuilder({ page })
    .withTags(['wcag2a','wcag2aa','wcag21a','wcag21aa','best-practice'])
    .analyze();
  expect(results.violations).toEqual([]);
});
```

<!-- The 7 routes to scan (one representative /work/[category] + one /watch/[id]): -->
/, /work, /work/pbs-american-portrait (a category slug), /watch/264677021 (producerReelId),
/pbs-american-portrait, /press, /about, /contact
(playwright.config.ts runs the preview server on port 4183, unprefixed — page.goto('/...') resolves at root)

<!-- POL-03 structural compliance to grep-verify (already satisfied). -->
ReelSection.svelte / PosterImage.svelte / PreviewLoop.svelte share `aspect-video w-full`;
ReelStage uses `h-svh` / `calc(100svh - …)`. The gate: 0 occurrences of `100vh` or `100dvh` in
snap-section components, ≥ 1 occurrence of `svh`.

<!-- LHCI / Lighthouse versions (verified npm 2026-05-28): @lhci/cli 0.15.1, lighthouse 13.3.0.
     Either is acceptable per D-17 Claude's Discretion. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden axe.spec.ts from /-only to all 7 routes (fix any surfaced WCAG AA violations)</name>
  <files>tests/e2e/axe.spec.ts (plus any source components needing a11y fixes: src/lib/components/PreviewLoop.svelte, WatchPlayer.svelte, FilterPillBar.svelte, CategoryTag.svelte)</files>
  <read_first>
    - tests/e2e/axe.spec.ts (current /-only scan — the pattern to extend)
    - playwright.config.ts (confirm preview server port 4183 + unprefixed base so page.goto('/work') resolves)
    - src/lib/components/CategoryTag.svelte AND src/lib/components/FilterPillBar.svelte (Pitfall 7: accent-token contrast + sticky-pill aria-current candidates)
    - src/lib/components/PreviewLoop.svelte (Pitfall 7: iframe needs title attribute)
    - CLAUDE.md §Accessibility (iframe title, aria-current="page" on active pill, opacity-not-display on chrome-fade, allow="autoplay; fullscreen; picture-in-picture")
  </read_first>
  <action>
    Rewrite `tests/e2e/axe.spec.ts` to scan all 7 routes with the same WCAG tag set. Concrete shape — a parametrized loop over the route list:
    ```typescript
    const ROUTES = [
      { name: 'home /', path: '/' },
      { name: 'work', path: '/work' },
      { name: 'work category (pbs)', path: '/work/pbs-american-portrait' },
      { name: 'watch (producer reel)', path: '/watch/264677021' },
      { name: 'pbs landing', path: '/pbs-american-portrait' },
      { name: 'press', path: '/press' },
      { name: 'about', path: '/about' },
      { name: 'contact', path: '/contact' },
    ];
    for (const route of ROUTES) {
      test(`${route.name} has zero axe WCAG AA violations`, async ({ page }) => {
        await page.goto(route.path);
        const results = await new AxeBuilder({ page })
          .withTags(['wcag2a','wcag2aa','wcag21a','wcag21aa','best-practice'])
          .analyze();
        expect(results.violations).toEqual([]);
      });
    }
    ```
    Run `pnpm test:e2e`. Pitfall 7: hardening to 7 routes WILL likely surface real violations the /-only scan never exercised. EXPECT to fix them (budget the time, do not assume zero). Likely fixes:
    - iframe missing `title` → add `title` attribute to the Vimeo/YouTube iframe in PreviewLoop.svelte / WatchPlayer.svelte
    - active filter pill missing `aria-current="page"` → add to FilterPillBar.svelte
    - color-contrast on category accent tokens → bump the token to clear WCAG AA (4.5:1) per the established Phase 4 pattern (text-neutral-50 fixes documented in STATE.md)
    - any chrome-fade using display/visibility → confirm it's opacity-0 + pointer-events-none (NAV-01 a11y)
    Fix violations at their source component; re-run until `results.violations` is empty on all 7 routes. If a violation is a genuine false-positive that must be accepted, document the rationale + the specific rule id in the SUMMARY (do NOT blanket-disable axe rules).
  </action>
  <verify>
    <automated>pnpm test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "path: '/work'" tests/e2e/axe.spec.ts` ≥ 1 AND the spec references all 7 routes (`/`, `/work`, `/work/`, `/watch/`, `/pbs-american-portrait`, `/press`, `/about`, `/contact` all appear)
    - `pnpm test:e2e` exits 0 with the axe tests green on every route (violations == [] for all 7)
    - Any source component fixes (iframe title, aria-current, contrast) are committed; no axe rule blanket-disabled without a documented per-rule rationale in the SUMMARY
  </acceptance_criteria>
  <done>axe scans all 7 routes; all WCAG AA violations fixed at source; test:e2e green.</done>
</task>

<task type="auto">
  <name>Task 2: POL-03 grep gate — confirm 100svh-only in snap sections, zero vh/dvh</name>
  <files>(no source edits expected — audit-only; record grep evidence in SUMMARY)</files>
  <read_first>
    - src/lib/components/ReelSection.svelte, PosterImage.svelte, PreviewLoop.svelte (the shared aspect-video w-full container — POL-03 zero-CLS contract)
    - src/lib/components/ReelStage.svelte (the snap container — h-svh / calc(100svh - …) usage)
    - src/app.css (any global section-height utilities)
    - POL-03 in REQUIREMENTS.md + 07-VALIDATION.md row (the grep contract: 100svh present, 100vh/100dvh absent in snap sections)
  </read_first>
  <action>
    POL-03 is ALREADY structurally satisfied (RESEARCH + Phase 3/4 decisions) — this task PROVES and PINS it, it does NOT refactor. Two checks:
    1. Run the grep gate manually across the reel/snap components + routes:
       - `grep -rn '100svh\|h-svh\|svh' src/lib/components src/routes src/app.css` → must return ≥ 1 (svh is in use)
       - `grep -rn '100vh\|100dvh\|h-dvh\|dvh' src/lib/components src/routes src/app.css` → must return 0 in scroll-snap section heights. (If any `dvh` exists on a NON-snap content block that should fill — e.g. a footer region — it is permitted per CLAUDE.md "100dvh for content blocks that should fill"; document any such intentional dvh in the SUMMARY. The ZERO target applies to snap-section heights only.)
    2. Confirm the poster→iframe zero-CLS contract structurally: `grep -l 'aspect-video' src/lib/components/ReelSection.svelte src/lib/components/PosterImage.svelte src/lib/components/PreviewLoop.svelte` — the shared `aspect-video w-full` container is what holds CLS at 0 across the swap. Confirm all three share it.
    The Lighthouse CLS number (`cumulative-layout-shift` ≈ 0) is confirmed in Task 3's `07-LIGHTHOUSE.json`.
    Record the grep outputs + the aspect-video confirmation in the SUMMARY as the POL-03 proof.
  </action>
  <verify>
    <automated>grep -rn "100vh\|100dvh" src/lib/components src/routes | grep -i "svh" -v | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc '100vh\|100dvh' src/lib/components src/routes` returns 0 matches in scroll-snap section-height declarations (any dvh on intentional fill-content blocks is documented in SUMMARY)
    - `grep -rc 'svh' src/lib/components src/routes` returns ≥ 1
    - `grep -l 'aspect-video' src/lib/components/ReelSection.svelte src/lib/components/PosterImage.svelte src/lib/components/PreviewLoop.svelte` lists all three files (shared zero-CLS container confirmed)
    - SUMMARY records the grep outputs as the POL-03 audit evidence
  </acceptance_criteria>
  <done>100svh-only confirmed in snap sections; aspect-video shared container confirmed across the three swap components; POL-03 audit evidence recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Measure / LCP on simulated 4G (D-17 measure-first) — lighthouserc.json + 07-LIGHTHOUSE.json</name>
  <files>lighthouserc.json, .planning/phases/07-polish-cutover/07-LIGHTHOUSE.json</files>
  <read_first>
    - src/routes/+page.svelte AND src/lib/components/HeroAmbient.svelte (the / LCP element is the hero POSTER image; the hero iframe is deferred via createHeroDefer until interaction/1s idle — Pitfall 3: _four's WebP <picture>+AVIF escalation does NOT map)
    - ../michelle_ngo_four/.planning/phases/07-polish-production-cutover/07-04-perf-gate-PLAN.md (the LHCI wiring + the mobile-preset trap: do NOT use preset:desktop, which disables Slow-4G and would hide a mobile failure)
    - ../michelle_ngo_four/.planning/phases/07-polish-production-cutover/07-LIGHTHOUSE.json (the report-artifact shape D-17 produces)
    - 07-RESEARCH.md §"@lhci/cli config for the / LCP gate" (the lighthouserc.json shape + the 2500 vs 2000 budget note)
  </read_first>
  <action>
    Create `lighthouserc.json` asserting the `/` LCP budget (D-17: < 2.5s = 2500ms on simulated 4G; looser than `_four`'s 2000). Concrete config (warning-only INITIAL posture — Plan 07-04 flips to blocking pre-cutover):
    ```json
    {
      "ci": {
        "collect": {
          "url": ["https://wolfwdavid.github.io/michelle_ngo_three/"],
          "numberOfRuns": 3
        },
        "assert": {
          "assertions": {
            "categories:performance": ["warn", { "minScore": 0.9 }],
            "largest-contentful-paint": ["warn", { "maxNumericValue": 2500 }],
            "cumulative-layout-shift": ["warn", { "maxNumericValue": 0.1 }]
          }
        }
      }
    }
    ```
    CRITICAL (Pitfall / _four 07-04 trap): do NOT set `"settings": { "preset": "desktop" }` — desktop disables Slow-4G throttle and would let a passing audit hide a real mobile LCP failure. LHCI's default collect is mobile + Slow-4G = exactly the D-17 "simulated 4G" profile. Leave the throttle at default.

    Run the actual measurement (D-17 "measure first" — do NOT ship a structured-deferral placeholder; that is the _four branch Pitfall 2 warns against). Use either `npx @lhci/cli@0.15.1 autorun` against the staging URL OR `npx lighthouse https://wolfwdavid.github.io/michelle_ngo_three/ --output=json --output-path=...` with mobile Slow-4G throttle. Capture the JSON and commit the real LCP + CLS numbers to `.planning/phases/07-polish-cutover/07-LIGHTHOUSE.json`.

    Inspect the measured `/` LCP. If ≤ 2500ms: record PASS, proceed (no escalation). If > 2500ms: do NOT silently accept — surface the number at Task 4's checkpoint with the poster-first escalation menu (NOT _four's AVIF <picture> route — Pitfall 3: escalate the POSTER path: preload the hero poster, ensure fetchpriority=high + loading=eager on it, AVIF poster variant [PERF-V2-02, deferred], or tune the createHeroDefer trigger timing).

    NOTE on staging-vs-prod: the staging URL is served under /michelle_ngo_three/ (BASE_PATH set); the production apex (BASE_PATH='') will measure marginally faster (no subpath). Document that the staging number is the conservative measurement.
  </action>
  <verify>
    <automated>pnpm build && test -f lighthouserc.json && grep -c "largest-contentful-paint" lighthouserc.json</automated>
  </verify>
  <acceptance_criteria>
    - `lighthouserc.json` exists AND `grep -c 'largest-contentful-paint' lighthouserc.json` == 1 AND `grep -c '2500' lighthouserc.json` ≥ 1
    - `lighthouserc.json` does NOT contain `"preset": "desktop"` (`grep -c 'desktop' lighthouserc.json` == 0)
    - `.planning/phases/07-polish-cutover/07-LIGHTHOUSE.json` exists AND contains a real measured `largest-contentful-paint` numericValue (NOT a deferral placeholder) — `grep -c 'largest-contentful-paint\|largestContentfulPaint' 07-LIGHTHOUSE.json` ≥ 1
    - The measured `/` LCP value is recorded in the SUMMARY with its pass/fail-vs-2500ms verdict
  </acceptance_criteria>
  <done>lighthouserc.json created (warning posture, mobile Slow-4G, 2500ms LCP); real measurement committed to 07-LIGHTHOUSE.json; LCP verdict recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Review / LCP measurement — escalate-or-accept decision (D-17)</name>
  <files>(no files — review gate; any escalation directed here is applied as a follow-up)</files>
  <action>Pause for the user to read the measured / LCP in 07-LIGHTHOUSE.json and decide escalate-or-accept per D-17. No files change in this task; if the user directs a poster-first escalation, apply it as a follow-up edit to the hero poster (preload / fetchpriority / defer-timing), NOT _four's AVIF route.</action>
  <what-built>
    A real Lighthouse measurement of the `/` LCP on simulated mobile 4G, committed to `07-LIGHTHOUSE.json`, plus the warning-only `lighthouserc.json` gate config (2500ms threshold). The axe gate now scans all 7 routes (green) and POL-03 100svh/aspect-video compliance is grep-confirmed.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/07-polish-cutover/07-LIGHTHOUSE.json` and read the measured `/` `largest-contentful-paint` numericValue.
    2. If LCP ≤ 2500ms: the poster-first budget holds — approve to keep the warning posture (Plan 07-04 will flip warn→error pre-cutover; the blocking flip is the cutover-readiness step).
    3. If LCP > 2500ms: decide escalate-or-accept. Escalation options (poster-first, NOT _four's AVIF <picture> — Pitfall 3): (a) preload the hero poster + fetchpriority=high, (b) AVIF poster variant (PERF-V2-02), (c) tune createHeroDefer timing, or (d) accept the looser number for staging and re-measure on the faster apex (BASE_PATH='') before the blocking flip. Direct which escalation (if any) to apply.
    Reference: D-17 says "measure first, escalate only if the poster path misses 2.5s." The _four branch (defer to post-launch real-user telemetry) is available as a fallback if you prefer not to escalate now.
  </how-to-verify>
  <resume-signal>Type "approved" (LCP passes, keep warning posture) OR name the escalation to apply OR "defer-to-telemetry" (the _four branch)</resume-signal>
  <acceptance_criteria>
    - User has reviewed the measured LCP number and either approved the warning posture or directed a specific escalation/deferral; the decision is recorded in the SUMMARY
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `pnpm test:e2e` green with axe scanning all 7 routes (violations == [] each)
- `grep -rc '100vh\|100dvh' src/lib/components src/routes` == 0 in snap-section heights; `svh` present
- `lighthouserc.json` exists (mobile Slow-4G, 2500ms LCP, warning posture, no desktop preset)
- `07-LIGHTHOUSE.json` holds a real measured LCP + CLS (not a deferral placeholder)
</verification>

<success_criteria>
- axe hardened from /-only to all 7 routes; latent WCAG AA violations fixed at source (POL-04 mechanical half)
- POL-03 grep-confirmed: 100svh-only in snap sections, shared aspect-video zero-CLS container
- POL-02 D-17 measure-first executed: real / LCP committed to 07-LIGHTHOUSE.json; escalate-or-accept resolved at checkpoint
- lighthouserc.json ready for Plan 07-04 to wire into CI (warning→blocking)
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-cutover/07-03-SUMMARY.md`
</output>
