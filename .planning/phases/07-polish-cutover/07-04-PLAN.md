---
phase: 07-polish-cutover
plan: 04
type: execute
wave: 2
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - .github/workflows/deploy.yml
  - src/lib/data/.four-route-manifest
autonomous: true
requirements: [POL-02, POL-04, POL-05]
nyquist_compliant: true

must_haves:
  truths:
    - "CI fails if the prerendered build drops below 70 sitemap URLs or is missing a required static asset (prerender-coverage gate wired)"
    - "CI fails if _three's prerendered route IA diverges from _four's pinned route manifest (Trap E)"
    - "CI fails if _three's OG image is not 1200x630 or wildly diverges in filesize from _four's (Trap B)"
    - "CI measures the / LCP on simulated 4G and reports against the 2500ms budget (Lighthouse gate, warning posture)"
    - "The existing Trap A (videos.json drift) + Trap D (localStorage namespace) gates still pass unchanged"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "Extended CI: prerender-coverage assert + Trap E route-manifest diff + Trap B OG-dim diff + Lighthouse LCP gate, alongside existing Trap A/D"
      contains: "test-prerender-coverage"
    - path: "src/lib/data/.four-route-manifest"
      provides: "Pinned _four prerendered-route list snapshot (Trap E baseline, sync-disciplined like .videos-source-sha)"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "scripts/test-prerender-coverage.mjs"
      via: "post-build CI step running the coverage assertion"
      pattern: "test-prerender-coverage"
    - from: ".github/workflows/deploy.yml"
      to: "__four/ pinned-SHA checkout"
      via: "Trap E + Trap B reuse the drift-check checkout mechanism"
      pattern: "michelle_ngo_four"
    - from: ".github/workflows/deploy.yml"
      to: "lighthouserc.json"
      via: "lhci autorun CI step against the deployed build"
      pattern: "lhci\\|lighthouse"
---

<objective>
Wire all the new mechanical CI gates into `.github/workflows/deploy.yml` in ONE plan (single-owner of deploy.yml to avoid parallel-write conflicts): the prerender-coverage assertion (pins 70-URL sitemap + static assets, D-14), the Trap E route-manifest diff (D-04), the Trap B OG-dimension parity diff (D-02/POL-05), and the Lighthouse `/` LCP gate (D-17/POL-02, warning posture). Verify the existing Trap A (videos.json drift) + Trap D (localStorage grep) gates still pass unchanged.

Purpose: D-01 says extend `_three`'s proven mechanical-gate patterns (the `drift-check` `__four/` pinned-SHA checkout + the D-17 `::error::` grep gate) to every trap that reduces to a script. This plan is the CI-orchestration consolidation — all gates land in the one workflow file so the Wave-1 plans (which produced the artifacts these gates assert against) stay file-isolated and parallel-safe.
Output: extended `deploy.yml` + `.four-route-manifest` sidecar.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-polish-cutover/07-CONTEXT.md
@.planning/phases/07-polish-cutover/07-RESEARCH.md
@.planning/phases/07-polish-cutover/07-VALIDATION.md

<interfaces>
<!-- Existing deploy.yml structure (VERIFIED) — extend it, do not rewrite. -->
.github/workflows/deploy.yml jobs today:
  build: (D-17 grep gate Trap D) → lint → check → test → playwright install → test:e2e (incl. axe) → build → upload artifact
  deploy: deploy-pages
  drift-check: (Trap A) reads src/lib/data/.videos-source-sha → checkout _four@SHA into __four/ → cmp videos.json → verify sidecar sha256

<!-- The .videos-source-sha sidecar (VERIFIED present) — the pinned _four SHA the Trap E manifest must match. -->
src/lib/data/.videos-source-sha:
  four_commit_sha=07667658ee2fd16a3d56b66bbe832d08fc3badd5
  videos_json_sha256=fd15e0568425ef8a8472b8bae856bc43a5a85810e4fb1a8f9d0cff771d8ef91c
  synced_at=2026-05-25

<!-- Route parity ground-truth (VERIFIED): _three and _four route trees are IDENTICAL except
     _three was missing /sitemap.xml/+server.ts — which Plan 07-01 now adds. After 07-01,
     route parity is EXACT. Expected prerendered build route dirs (both repos): -->
/, /work, /work/<8 category slugs>, /watch/<56 ids>, /pbs-american-portrait, /press, /about, /contact
+ sitemap.xml (file, not a dir)

<!-- Artifacts these gates assert against (produced by Wave-1 plans): -->
- scripts/test-prerender-coverage.mjs + test:prerender npm script (Plan 07-01)
- static/og-image.jpg @ 1200x630 (Plan 07-02) ; __four/static/og-image.jpg @ 1200x630 ~15386 bytes (Trap B target)
- lighthouserc.json (Plan 07-03, warning posture, mobile Slow-4G, 2500ms LCP)

<!-- _four reference for the gate mechanism. -->
The drift-check job ALREADY checks out __four/ at the pinned SHA — Trap E + Trap B reuse that checkout.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .four-route-manifest sidecar + wire prerender-coverage + Trap E route-manifest diff into deploy.yml</name>
  <files>src/lib/data/.four-route-manifest, .github/workflows/deploy.yml</files>
  <read_first>
    - .github/workflows/deploy.yml (the build job + drift-check job — the exact step shape + the __four/ checkout to reuse)
    - src/lib/data/.videos-source-sha (the sidecar sync-discipline pattern to mirror for .four-route-manifest)
    - scripts/test-prerender-coverage.mjs (Plan 07-01's coverage script — the post-build assertion to wire)
    - 07-RESEARCH.md §"Pattern 4: Mechanical CI drift gate" + §"Trap E route-manifest diff" (the diff mechanism + the committed-snapshot-vs-build-_four tradeoff; recommendation: committed snapshot, avoids building _four in CI)
  </read_first>
  <action>
    Two additions:

    (A) Create `src/lib/data/.four-route-manifest` — a committed snapshot of `_four`'s prerendered route list (the Trap E baseline, mirroring the `.videos-source-sha` sync-discipline). Generate it from `_four`'s actual route tree (VERIFIED identical to `_three` post-07-01). Content = a sorted list of expected prerendered route paths, one per line, e.g.:
    ```
    # _four prerendered route manifest — pinned at four_commit_sha=07667658ee2fd16a3d56b66bbe832d08fc3badd5
    # Trap E (D-04): _three's prerendered routes MUST match this set. Refresh when _four's IA changes.
    /
    /about
    /contact
    /pbs-american-portrait
    /press
    /watch  (parameterized — 56 ids)
    /work
    /work  (parameterized — 8 category slugs)
    ```
    Use the route-DIRECTORY shape the CI diff produces (see B). Pick the exact normalization (strip /index.html, strip the [id]/[category] explosions to their parent OR enumerate counts) and document it inline so the diff is deterministic. Simplest deterministic form: the sorted set of top-level prerendered route SHAPES — `/`, `/about`, `/contact`, `/pbs-american-portrait`, `/press`, `/watch/*` (count 56), `/work` (1), `/work/*` (count 8). Encode whatever shape the Task-B diff command consumes.

    (B) In `deploy.yml` build job, AFTER the `Build` step, add:
    - `Prerender coverage assertion` step: `run: node scripts/test-prerender-coverage.mjs` (pins the 70-URL sitemap + favicon set + og-image — D-14). Fails the build if sitemap < 70 or any required asset missing.
    - `Trap E — route-manifest diff` step: enumerate `_three`'s prerendered route shapes from `build/` (`find build -name index.html | sed 's|/index.html||' | sed 's|^build||' | sort`, then normalize the 56 `/watch/<id>` to a `/watch/*` shape + count==56 and the 8 `/work/<slug>` to `/work/*` count==8 to match the manifest's normalization), and `diff` against `src/lib/data/.four-route-manifest`. On divergence: `echo "::error::Trap E — route IA divergence between _three and _four@<sha>. The A/B isolates DESIGN, not IA. Re-sync src/lib/data/.four-route-manifest if _four's routes legitimately changed."` + print the diff + `exit 1`.

    Use the committed-snapshot approach (NOT building `_four` in CI) per RESEARCH Open Question 3 recommendation — faster, sync-disciplined like the videos sidecar. The drift-check job's `__four/` checkout is NOT needed for Trap E with this approach.
  </action>
  <verify>
    <automated>pnpm build && node scripts/test-prerender-coverage.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/data/.four-route-manifest` exists AND contains the 6 static route shapes + `/watch` + `/work` references
    - `.github/workflows/deploy.yml` contains a `node scripts/test-prerender-coverage.mjs` step (`grep -c 'test-prerender-coverage' .github/workflows/deploy.yml` ≥ 1)
    - `.github/workflows/deploy.yml` contains a Trap E step (`grep -c 'Trap E' .github/workflows/deploy.yml` ≥ 1 AND `grep -c 'four-route-manifest' .github/workflows/deploy.yml` ≥ 1 AND `grep -c '::error::' .github/workflows/deploy.yml` ≥ 1 for the new gate)
    - Locally: `pnpm build && node scripts/test-prerender-coverage.mjs` exits 0 (70-URL sitemap + all static assets present — depends on 07-01 + 07-02 landed)
    - The Trap E diff command, run locally against the current build, produces zero divergence vs `.four-route-manifest` (route parity holds — verified by the build having exactly the manifest's route shapes)
  </acceptance_criteria>
  <done>.four-route-manifest sidecar committed; prerender-coverage + Trap E gates wired into deploy.yml; both pass locally.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Trap B OG-dimension diff + Lighthouse LCP gate into deploy.yml; verify Trap A/D still pass</name>
  <files>.github/workflows/deploy.yml</files>
  <read_first>
    - .github/workflows/deploy.yml (the drift-check job's __four/ checkout — Trap B reuses it; the existing D-17 grep gate Trap D + cmp Trap A to leave unchanged)
    - static/og-image.jpg (Plan 07-02's asset — 1200x630, the Trap B subject)
    - lighthouserc.json (Plan 07-03's config — warning posture, mobile Slow-4G, 2500ms LCP)
    - 07-RESEARCH.md §"Trap B — OG dimension parity diff" (the filesize-ratio + dimension-grep mechanism) + §"@lhci/cli config" (the LHCI CI invocation + the mobile-preset trap)
  </read_first>
  <action>
    Three additions/confirmations:

    (A) Trap B — OG dimension parity (D-02/POL-05). Add a CI step that reuses the drift-check job's `__four/` checkout (or its own checkout of `_four` at the pinned SHA from `.videos-source-sha`). Assert:
    - `_three`'s `static/og-image.jpg` is exactly 1200×630 — probe via a node sharp metadata check (sharp is on disk): `node -e "require('sharp')('static/og-image.jpg').metadata().then(m=>{if(m.width!==1200||m.height!==630){console.error('::error::Trap B — _three og-image.jpg is '+m.width+'x'+m.height+', must be 1200x630 for A/B social-crop parity');process.exit(1)}console.log('Trap B dimensions OK 1200x630')})"`
    - filesize comparable to `_four`'s `__four/static/og-image.jpg` (ratio band ≤ 3×): the filesize-ratio guard from RESEARCH (statSync both, `if (ratio > 3) ::error:: + exit 1`).
    - Also grep the built `build/index.html` for the meta-tag half: `grep -c 'og:image:width" content="1200"' build/index.html` ≥ 1 AND `grep -c 'og:image:height" content="630"' build/index.html` ≥ 1.
    Place this in the drift-check job (which already has `__four/`) so the `__four/` checkout is reused (D-04 discretion: "reuse the existing __four/ checkout").

    (B) Lighthouse LCP gate (D-17/POL-02). Add a new job `lighthouse` (or a step after deploy) running `npx @lhci/cli@0.15.1 autorun` against the staging URL using `lighthouserc.json` (warning posture — `["warn", { maxNumericValue: 2500 }]`, so it does NOT fail the build yet; it surfaces the LCP number in CI logs). The blocking flip (warn→error) is a documented pre-cutover step recorded in Plan 07-05's runbook + QA-matrix (D-17 "blocking pre-cutover"). Do NOT use preset:desktop (the _four 07-04 trap — would disable Slow-4G). Pin `@lhci/cli@0.15.1`.

    (C) Verify-not-rebuild Trap A + Trap D: confirm the existing `drift-check` cmp step (Trap A) and the `D-17 grep gate` step (Trap D) are UNCHANGED and still present. Do not touch their logic.

    Keep `concurrency.group: pages` and the existing build/deploy jobs intact. The new gates must not break the staging deploy.
  </action>
  <verify>
    <automated>pnpm build && grep -c "Trap B" .github/workflows/deploy.yml && grep -c "lighthouserc\|lhci\|lighthouse" .github/workflows/deploy.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/deploy.yml` contains a Trap B step (`grep -c 'Trap B' .github/workflows/deploy.yml` ≥ 1) probing og-image 1200x630 + filesize ratio vs `__four/`
    - `.github/workflows/deploy.yml` contains a Lighthouse step (`grep -c 'lhci\|lighthouse' .github/workflows/deploy.yml` ≥ 1) referencing `lighthouserc.json` and pinning `@lhci/cli@0.15.1`
    - `grep -c 'desktop' .github/workflows/deploy.yml` shows no `preset: desktop` introduced for the LHCI run (the mobile-Slow-4G default is preserved)
    - Trap A cmp step still present: `grep -c 'byte-identical to _four\|cmp -s' .github/workflows/deploy.yml` ≥ 1 (unchanged)
    - Trap D grep gate still present: `grep -c 'D-17 grep gate' .github/workflows/deploy.yml` ≥ 1 (unchanged)
    - Locally the Trap B dimension probe exits 0 against `static/og-image.jpg` (1200x630 confirmed)
    - `pnpm build` exits 0 (the workflow YAML is valid + the gates do not break the build)
  </acceptance_criteria>
  <done>Trap B OG-dim diff + Lighthouse warning gate wired into deploy.yml; Trap A/D confirmed unchanged; build green.</done>
</task>

</tasks>

<verification>
- `deploy.yml` now runs: D-17 grep gate (Trap D, unchanged) → lint → check → test → e2e (axe 7-route) → build → prerender-coverage → Trap E diff → upload/deploy; drift-check job runs Trap A (unchanged) + Trap B OG-dim; lighthouse job runs the warning-posture LCP gate
- `pnpm build && node scripts/test-prerender-coverage.mjs` exits 0 (70 URLs + assets)
- Trap E diff against `.four-route-manifest` = zero divergence
- Trap B og-image probe = 1200×630, filesize within 3× of _four
- Trap A + Trap D gates present and unchanged
</verification>

<success_criteria>
- All 5 A/B-integrity traps are mechanically gated or verified: A (unchanged, verified) + B (new OG-dim) + C (policy — confirmed in 07-05) + D (unchanged, verified) + E (new route-manifest diff)
- prerender-coverage assertion pins the 70-URL sitemap + static assets in CI (D-14)
- Lighthouse `/` LCP gate runs in CI at warning posture (D-17/POL-02); blocking flip documented for cutover
- deploy.yml is single-owner-edited this phase (no parallel-write conflict)
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-cutover/07-04-SUMMARY.md`
</output>
